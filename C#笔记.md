# 速查



# 高频 C# 代码片段速查（记一下，超级有用）

你也可以直接打这些缩写，然后按 **两次 `Tab`**：

| 你输入的缩写 | 生成的代码模板                         | 备注             |
| :----------- | :------------------------------------- | :--------------- |
| `cw`         | `Console.WriteLine();`                 | 最常用的调试输出 |
| `for`        | `for (int i = 0; i < length; i++){ }`  | 标准计数循环     |
| `foreach`    | `foreach (var item in collection){ }`  | 遍历集合         |
| `while`      | `while (true){ }`                      | 条件循环         |
| `switch`     | `switch (switch_on){ default: }`       | 多分支判断       |
| `try`        | `try { } catch (Exception) { throw; }` | 异常捕获         |
| `tryf`       | `try { } finally { }`                  | 异常+最终执行    |
| `prop`       | `public int MyProperty { get; set; }`  | 自动属性         |
| `ctor`       | `public ClassName(){ }`                | 构造函数         |



好的，明白。既然你想把这份笔记当作以后查阅的 **“终极字典”** 和 **“复习大纲”**，那我将**所有对话内容**拆解重构，按 **“由浅入深、由理论到实战”** 的逻辑，整理为 **9 大核心篇章**。

这份笔记涵盖了 **.NET 基础、计算机底层、数据类型、运算符、流程控制、数组、面向对象** 的全部精华，并补全了所有边缘细节和坑点，你可以放心收藏。

---

# C# 与 .NET 从零到进阶·终极全栈笔记（对话全收录）


## 第一篇：.NET 平台与开发工具（环境与认知）

### 1. .NET 家族史（Framework vs Core vs 现代 .NET）

- **.NET Framework**（旧时代）：仅限 Windows 系统，系统级安装（全局版本唯一），部分闭源。目前微软已停止新特性开发，仅做安全维护。
- **现代 .NET（.NET Core / .NET 5/6/7/8+）**（新时代）：**完全开源**，**跨平台**（Windows、Linux、macOS），支持**应用级安装**（不同项目可依赖不同版本，互不干扰）。性能极高，云原生首选。
- **选择铁律**：**所有新项目必须使用现代 .NET**；只有维护十几年前的旧 WinForm 项目才保留 Framework。

### 2. 开发利刃：CodeRush 插件（高频快捷键大全）

| 功能模块                 | 核心用途                                               | 触发快捷键 / 操作                                            |
| :----------------------- | :----------------------------------------------------- | :----------------------------------------------------------- |
| **代码模板展开**         | 输入缩写快速生成代码块                                 | 新手按 **`Tab`**；追求极致效率按 **`Space`（空格）**         |
| **跳转到下一引用**       | 在文件/解决方案中跳转到同名变量引用处                  | **`Tab`**（下一处），**`Shift+Tab`**（上一处），**`Esc`**（取消返回），**`Enter`**（停留并清除高亮） |
| **拼写检查**             | 检查注释、字符串、标识符拼写                           | 光标置于词上，按 **`Ctrl + .`**（或数字键盘 **`Num 0`**）调出修正菜单 |
| **调试可视化**           | 调试时钻取复杂表达式，高亮布尔逻辑，预测下一步执行位置 | 当前行按 **`Alt + ↓`**（下箭头）钻取，用 **方向键（↑↓←→）** 导航层级 |
| **选择嵌入（包裹代码）** | 选中多行代码，快速包裹结构                             | **`b`**（大括号 `{}`）、**`c`**（try/catch）、**`f`**（try/finally）、**`t`**（try/catch/finally） |


## 第二篇：计算机底层原理（内存与进制）

### 1. 二进制与补码规则（以 8 位有符号整数为例）

- **符号位**：最高位（最左边）0 代表正数，1 代表负数。
- **补码存储**：计算机存储整数采用补码（为了统一加减法电路）。
- **特殊编码 `1000 0000`**：在“原码”中代表“负0”（无意义），在“补码”中**被硬性规定为 -128**。
- **取值范围推导**：`-128` ~ `+127`（共 256 个数字，因为 2^8 = 256）。
- **负数转换算法**：正数取反加 1。例：`+1 = 0000 0001`，取反 `1111 1110`，加 1 得 `1111 1111`（即 -1）。

### 2. 存储单位（bit / Byte / KB ... PB）

| 单位           | 符号       | 本质                  | 换算关系         |
| :------------- | :--------- | :-------------------- | :--------------- |
| **比特（位）** | **小写 b** | 最小单元，只存 0 或 1 | 1 Byte = **8** b |
| **字节**       | **大写 B** | 计算机基本操作单位    | **核心基准**     |
| 千字节         | KB         | 2^10 Byte             | 1 KB = 1024 B    |
| 兆字节         | MB         | 2^10 KB               | 1 MB = 1024 KB   |
| 吉字节         | GB         | 2^10 MB               | 1 GB = 1024 MB   |
| 太字节         | TB         | 2^10 GB               | 1 TB = 1024 GB   |
| 拍字节         | PB         | 2^10 TB               | 1 PB = 1024 TB   |

- **实战避坑**：运营商宽带 100 **Mbps**（小写 b，兆比特）÷ 8 = 理论最大下载速度 **12.5 MB/s**（大写 B，兆字节）。大小写搞错，速度差 8 倍！


## 第三篇：C# 类型系统（值类型 vs 引用类型 彻底通关）

### 1. 值类型（存于栈 Stack）

- **特点**：变量直接存储数据本身；赋值即复制完整数据；修改互不影响。
- **包含**：`int`, `long`, `float`, `double`, `bool`, `char`, `byte`, `decimal`，以及自定义的 **`struct`（结构体）** 和 **`enum`（枚举）**。

### 2. 引用类型（存于堆 Heap）

- **特点**：变量存储的是**堆内存的门牌号（地址）**；赋值只复制地址，多个变量可能指向同一对象；修改会相互影响。
- **包含**：`string`（字符串）、**数组**（`Array`）、自定义 **`class`（类）**。

### 3. 基本数据类型全家桶（内存占用与范围清单）

| 类型     | 关键字    | 内存       | 取值范围 / 精度           | 特殊说明                                                     |
| :------- | :-------- | :--------- | :------------------------ | :----------------------------------------------------------- |
| **整型** | `int`     | 4 字节     | ±21 亿                    | **默认整数首选**                                             |
|          | `long`    | 8 字节     | ±922 亿亿                 | 超大数（对应 C++ 的 `long long`）                            |
|          | `byte`    | 1 字节     | 0 ~ 255                   | 无符号，文件流专用                                           |
|          | `sbyte`   | 1 字节     | -128 ~ 127                | 有符号（即补码经典范围）                                     |
| **浮点** | `double`  | 8 字节     | 15-17 位有效数字          | **小数默认类型**（如 `3.14`）                                |
|          | `float`   | 4 字节     | 6-9 位有效数字            | 必须加后缀 `f`（如 `3.14f`）                                 |
|          | `decimal` | 16 字节    | 28-29 位有效数字          | **金融/金额专用**，必须加后缀 `m`（如 `3.14m`），绝对精确无二进制误差 |
| **文本** | `char`    | **2 字节** | 0 ~ 65535（Unicode 码点） | **值类型**，存单个字符（如 `'A'`），本质存的是 UTF-16 编码数字 |
|          | `string`  | 可变       | 字符序列                  | **引用类型**，**不可变性（Immutable）**：修改字符串实际是在堆上新建对象 |
| **逻辑** | `bool`    | 1 字节     | `true` / `false`          | 条件判断专用                                                 |

### 4. char 的本质（深入底层）

- `char` 虽然是值类型，但其底层核心是一个**无符号整数**（码点）。
- `(char)97` 会去 Unicode 全球字符表里查找 97 号对应的图形，得到字母 `'a'`（因为 `'a'` 的 ASCII 编码就是 97）。
- `(char)11101` 会查表得到特殊符号 `⭝`（十六进制 0x2B5D）。

### 5. 类型转换三大方法（深度辨析）⭐

| 方法族               | 写法示例                   | 对 `null` 的处理       | 对格式的要求                           | 本质                         |
| :------------------- | :------------------------- | :--------------------- | :------------------------------------- | :--------------------------- |
| **`.Parse()`**       | `int.Parse("123")`         | **抛出异常**（崩溃）   | **极其严格**（`" 123 "` 带空格都不行） | 特定类型的静态方法           |
| **`System.Convert`** | `Convert.ToInt32(" 123 ")` | **返回默认值**（如 0） | **较宽容**（自动去空格）               | 万能工具类方法               |
| **`.ToString()`**    | `num.ToString()`           | `null` 调用会**崩溃**  | 永不出错（万物皆可转文本）             | 所有类型继承自 Object 的方法 |

- **特殊记忆**：`(char)num`（强转）是内存级别的重新解释；`Convert.ToChar(num)` 是方法调用，会检查溢出（超出 0~65535 会报错）。

### 6. 自定义类比较的陷阱（面试必考）

- **默认行为**：对于自定义 `class`（如 `Person`），使用 `==` 或 `Equals()` 比较的是**内存地址（门牌号）**。

- **验证**：

  ```csharp
  Person p1 = new Person{ Name="A" };
  Person p2 = new Person{ Name="A" };
  bool result = (p1 == p2); // 结果为 False（因为地址不同）
  ```

- **为什么 `string` 比较内容？** 因为微软在 `string` 类内部**重写了（Override）** `Equals` 和 `==` 运算符，改为比较字符串内部的字符序列。

- **解决方案**：如果想让自定义类比较内容，必须在类内部重写 `Equals` + `GetHashCode` 方法。


## 第四篇：C# 运算符与表达式（细节与坑）

### 1. 算术与关系运算符

- **算术**：`+` `-` `*` `/` `%`（取余）。
- **关系**：`>` `<` `>=` `<=` `==`（相等） `!=`（不等）。
- **整数除法警告**：`int` / `int` 结果只取整数部分（截断）。例：`8 / 6 = 1`（不是 1.33），若要精确需转为浮点：`8.0 / 6`。

### 2. 逻辑运算符与短路运算（核心难点）

| 符号   | 名称             | 逻辑（针对 bool）                    | 特殊补充（针对整数）                    |
| :----- | :--------------- | :----------------------------------- | :-------------------------------------- |
| `&`    | 逻辑与（非短路） | 无论左边真假，**右边一定会执行**     | 可用于整数**按位与**（如 `5 & 3 = 1`）  |
| `&&`   | 短路与           | **左边为 false，右边直接跳过不执行** | 仅适用于 bool                           |
| `\|`   | 逻辑或（非短路） | 无论左边真假，**右边一定会执行**     | 可用于整数**按位或**（如 `5 \| 3 = 7`） |
| `\|\|` | 短路或           | **左边为 true，右边直接跳过不执行**  | 仅适用于 bool                           |

**短路运算符的实际意义**：

- **优点 1（性能）**：减少不必要的计算。
- **优点 2（安全，极其重要）**：避免空引用异常。`if (user != null && user.Age > 18)` 当 user 为 null 时，右边被短路，程序不崩溃。
- **缺点（副作用）**：若右侧包含计算逻辑（如 `++j`），短路会导致该计算被跳过，引发业务 Bug。

### 3. 自增自减复杂表达式推演（C# 规范）

- **表达式**：`number++ * ++number - number-- / --number` （初始 `number = 6`）
  - ① 左操作数：`number++`（后置）→ 取 6，变 7。
  - ② 右操作数：`++number`（前置）→ 变 8，取 8。
  - ③ 乘法结果：`6 * 8 = 48`。
  - ④ 左操作数：`number--`（后置）→ 取 8，变 7。
  - ⑤ 右操作数：`--number`（前置）→ 变 6，取 6。
  - ⑥ 除法结果：`8 / 6 = 1`（整数截断）。
  - ⑦ 最终表达式结果：`48 - 1 = 47`，变量最终值变回 **`6`**。
- **跨语言差异**：**在 C/C++ 中，这是未定义行为（结果随编译器不同）**。
- **实战铁律**：此类复合自增减表达式属于**严重的“坏味道代码”**，严禁出现在项目代码中，请拆分为多行清晰语句。

### 4. 代码坏味道（Code Smell）概念

- **定义**：代码运行正常，但结构设计极差（定时炸弹），在未来需求变更时极易引发 Bug。
- **常见坏味道**：神秘的变量名、重复代码、过长函数、**在逻辑判断中偷偷修改变量**（如 `if` 里写 `++j`）。
- **救治办法**：**重构（Refactoring）**——在不改变功能的前提下，调整代码结构（如提取方法、拆分表达式）。


## 第五篇：流程控制语句（分支与循环）

### 1. 条件分支（选择结构）

- **三元运算符**：`条件 ? 真值 : 假值`（适用于简单的二选一赋值）。
- **if 语句（三种形式）**：
  - 单分支：`if(条件){ ... }`
  - 双分支：`if(条件){ ... } else { ... }`
  - 多分支：`if(条件1){ ... } else if(条件2){ ... } else { ... }`（互斥，遇真即停）。
- **switch 语句（多值匹配）**：
  - 结构：`switch(变量){ case 值1: ... break; case 值2: ... break; default: ... break; }`
  - **语法规则**：每个 `case` 末尾必须有 `break`（跳出），除非该 `case` 是**空的**（如 `case 1:` 下面直接写 `case 2:`，可以穿透）。
  - **⚠️ 浮点数禁用警告**：`switch` 语法上允许 `float`/`double`，但**绝对不要用**！因为浮点数有精度误差（如 `0.1+0.2 != 0.3`），等值匹配极易失败。判断浮点数相等应使用：`Math.Abs(num - 5.0) < 1e-10`。

### 2. 循环语句（迭代结构）

- **`while` 循环**：`while(条件){ 循环体 }`。条件为真则执行，需在循环体内改变条件变量，否则死循环。
- **`break` 关键字**：**跳出本层循环**，终止整个循环。通常与 `if` 搭配使用。
- **`continue` 关键字**：**结束本次循环**，跳过 `continue` 后续代码，进入下一次循环判断。通常与 `if` 搭配使用。


## 第六篇：数组（一维与二维）

### 1. 数组的本质

- **引用类型**（存储在堆内存）。数组变量存的是堆地址。
- **长度不可变**：一旦创建，`Length` 只读，无法扩容。

### 2. 一维数组的 4 种初始化写法（对照记）

| 写法               | 示例                             | 特点                     |
| :----------------- | :------------------------------- | :----------------------- |
| 动态（指定长度）   | `int[] arr = new int[5];`        | 元素为默认值（int 为 0） |
| 动态（长度+赋值）  | `int[] arr = new int[3]{1,2,3};` | 长度必须与元素个数一致   |
| 动态（省略长度）   | `int[] arr = new int[]{1,2,3};`  | 编译器自动推断长度       |
| **静态（最常用）** | `int[] arr = {1,2,3};`           | 语法糖，简洁明了         |

### 3. 二维数组的定义与访问

- **声明**：`int[,] arr;`
- **静态初始化**：`int[,] arr = { {1,2}, {3,4} };`（2行2列）
- **访问**：`arr[行下标, 列下标]`（下标从 0 开始）。
- **长度属性区分**：
  - `arr.Length`：返回**总元素个数**（行数 × 列数）。
  - `arr.GetLength(0)`：返回**行数**（第一维）。
  - `arr.GetLength(1)`：返回**列数**（第二维）。

### 4. 遍历技巧

- **`for` 循环**：可获取下标，**可修改**元素。优先使用（性能略好）。
- **`foreach` 循环**：**只读**，无法修改元素（`item` 不可赋值）。代码简洁，适合仅读取数据场景。


## 第七篇：排序算法（冒泡排序·优化版）

### 核心思想与优化标志位

- **逻辑**：相邻元素两两比较，前大后小则交换（升序）。每轮将最大值“冒泡”到末尾。
- **优化**：增加 `bool swapped` 标志位。若某一轮未发生任何交换，说明数组已经有序，**直接 `break` 跳出循环**，避免无效遍历。
- **时间复杂度**：最坏 O(n²)，最好（已有序）O(n)。


## 第八篇：面向对象（类、对象、字段）

### 1. 面向过程 vs 面向对象

- **面向过程**：关注“步骤”（做事的流程），性能极高但维护难、复用难。
- **面向对象（OOP）**：关注“参与者（对象）”及其“特性（字段）”和“行为（方法）”。
- **OOP 三大特征（必考）**：**封装**（隐藏内部细节）、**继承**（子类复用父类）、**多态**（同一方法不同表现）。

### 2. 类（Class）与对象（Object）

- **类 = 图纸**（不占内存，抽象模板）。
- **对象 = 实例**（`new` 出来，占用堆内存的具体实体）。
- **实例化语法**：`类名 变量名 = new 类名();`
- **内存图解**：`new` 在堆中开辟空间；变量在栈中存的是该堆空间的**内存地址（引用）**。

### 3. 字段（Field）

- **作用**：描述类的特性（属性）。
- **命名规范**：**小驼峰（camelCase）**，如 `studentName`。
- **默认值**：数字类为 0，`bool` 为 `false`，`string` 为 `null`。
- **访问修饰符**：
  - `public`：完全公开，任何地方可访问。
  - `private`：私有，仅类内部可访问（默认）。


## 第九篇：高级自定义类型（枚举、结构体）

### 1. 枚举（Enum）

- **定义**：一组命名常量的集合。**值类型**。
- **赋初值规则**：默认从 0 开始递增。可手动指定值（如 `Helmet = 100`），后续项自动 +1。
- **类型转换**：
  - 枚举→整数：`(int)枚举变量`。
  - 整数→枚举：`(枚举类型)整数值`（值不存在时不会报错，但需用 `Enum.IsDefined` 检查）。
- **应用场景**：表示状态、类型、选项（如性别、装备类型）。

### 2. 结构体（Struct）

- **定义**：轻量级数据组合。**值类型**。
- **与类的区别**：结构体是值类型（复制传递），类是引用类型（引用传递）。
- **构造函数铁律（极重要）**：
  - 开发者**不能手动定义无参构造函数**（系统自带）。
  - 自定义构造函数（带参）**必须给结构体中的所有字段赋值**，少一个都编译报错。
- **初始化方式**：
  - 不用 `new`：需手动给所有字段赋值后才能使用。
  - 用 `new`：调用系统默认构造，自动赋予默认值（如 int 为 0）。

### 3. 结构体与枚举实战（装备系统示例）

- 定义 `EquipType` 枚举（AD/AP/HP）。
- 定义 `HeroEquip` 结构体（Name, AD, AP, HP, Type）。
- 存入结构体数组，通过循环遍历找血量最高（冒泡思想），或按攻击力排序（冒泡排序对比结构体字段）。

---

**结语**：这份笔记涵盖了你我对话中所有的“是什么、为什么、怎么用、有什么坑”。以后复习或需要速查时，只需翻阅此 9 大篇章即可快速唤起所有记忆。如有新的知识点（如继承、多态、委托、异常处理），随时可以继续提问，我会为你开辟新的篇章。😊



# C#

## 数据的量级

## 计算机存储单位完整换算笔记（从最小到最大）

### 一、最核心的两个基础概念（必须先分清）

| 单位     | 英文全称     | 中文名称       | 本质定义                                                     | 符号（必须大写/小写区分） |
| :------- | :----------- | :------------- | :----------------------------------------------------------- | :------------------------ |
| **bit**  | Binary Digit | **比特（位）** | **计算机存储的最小单位**。它只能存储一个二进制位，即只能是 **0** 或者 **1**。它是纯粹的电信号开关（开/关）。 | **小写 `b`**              |
| **Byte** | -            | **字节**       | **计算机处理信息的基本单位**。它由 **8 个连续的 bit** 组成。计算机不会单独去处理一个 bit，而是以 8 个为一组（即 1 个 Byte）作为基本操作单元。 | **大写 `B`**              |

> **重要警示（99% 的新手都会搞混）**：
> 在购买宽带时，运营商说的“100 兆宽带”单位是 **Mbps（兆比特每秒）**，这里的 `b` 是小写（bit）。而下载工具显示的速度是 **MB/s（兆字节每秒）**，这里的 `B` 是大写（Byte）。
> **换算关系**：**1 MB/s = 8 Mbps**。所以 100 兆宽带的理论极限下载速度是 `100 ÷ 8 = 12.5 MB/s`。

------

### 二、完整的存储单位换算链条（补全版）

以下是从最小单位 `bit` 到最大单位 `PB` 的完整递进表：

| 换算关系            | 具体数值         | 备注（何时会遇到）                                           |
| :------------------ | :--------------- | :----------------------------------------------------------- |
| **1 Byte (1 字节)** | **= 8 bit**      | **计算机最基础的计量单位**。一个英文字母（如 `'a'`）占用 1 Byte。 |
| **1 KB (Kilobyte)** | **= 1024 Bytes** | **千字节**。一个简单的文本文件（如纯文本文档）大小。         |
| **1 MB (Megabyte)** | **= 1024 KB**    | **兆字节**。一张手机拍摄的高清照片大约 3~5 MB。              |
| **1 GB (Gigabyte)** | **= 1024 MB**    | **吉字节**。一部高清电影大约 1~2 GB。                        |
| **1 TB (Terabyte)** | **= 1024 GB**    | **太字节**。日常家用电脑硬盘容量（1T、2T）。                 |
| **1 PB (Petabyte)** | **= 1024 TB**    | **拍字节**。大型数据中心、云服务器集群的总存储量。           |

- 1024字节（byte）=1KB
- 1024KB = 1MB
- 1024MB = 1GB
- 1024GB = 1TB
- 1024TB = 1PB

## 数据类型

### 一、核心分类（最底层的记忆框架）

C# 中的所有类型分为两大类：

1. **值类型 (Value Types)**：存储在栈（Stack）中，速度快，赋值时复制完整数据。
2. **引用类型 (Reference Types)**：存储在堆（Heap）中，变量存的是内存地址，赋值时复制引用（地址）。

------

### 二、整数类型全家桶（按内存大小和有无符号排列）

| 关键字   | .NET 类型       | 有无符号 | 内存大小 | 取值范围（区间）                                        | 本质 / 用途                                                  |
| :------- | :-------------- | :------- | :------- | :------------------------------------------------------ | :----------------------------------------------------------- |
| `byte`   | `System.Byte`   | **无**   | 1 字节   | 0 ~ 255                                                 | 存储极小的非负整数，常用于文件流、图片原始数据。             |
| `sbyte`  | `System.SByte`  | **有**   | 1 字节   | -128 ~ +127                                             | 对应计算机底层 8 位补码的经典范围（您之前问的）。            |
| `short`  | `System.Int16`  | **有**   | 2 字节   | -32,768 ~ +32,767                                       | 短整数，底层协议或跨平台交互时使用。                         |
| `ushort` | `System.UInt16` | **无**   | 2 字节   | 0 ~ 65,535                                              | 无符号短整数。                                               |
| `int`    | `System.Int32`  | **有**   | 4 字节   | -2,147,483,648 ~ +2,147,483,647                         | **最常用的整数默认选择**。循环、数组索引、常规计数都用它。   |
| `uint`   | `System.UInt32` | **无**   | 4 字节   | 0 ~ 4,294,967,295                                       | 无符号整型，不常用（不符合公共语言规范 CLS）。               |
| `long`   | `System.Int64`  | **有**   | 8 字节   | -9,223,372,036,854,775,808 ~ +9,223,372,036,854,775,807 | **超大整数**。对应 C/C++ 里的 `long long`，超过 21 亿的计数或数据库大 ID 必须用它。 |
| `ulong`  | `System.UInt64` | **无**   | 8 字节   | 0 ~ 18,446,744,073,709,551,615                          | 最大的无符号整数。                                           |

**记忆口诀**：无符号带 `u`，`int` 是 4 字节主力，`long` 是 8 字节大哥。

------

### 三、浮点数（小数 / 实数）类型

| 关键字    | .NET 类型        | 内存大小    | 精度（有效数字） | 取值范围                     | 本质 / 用途                                                  |
| :-------- | :--------------- | :---------- | :--------------- | :--------------------------- | :----------------------------------------------------------- |
| `float`   | `System.Single`  | 4 字节      | ~6~9 位          | ±1.5 × 10⁻⁴⁵ ~ ±3.4 × 10³⁸   | **单精度浮点数**。内存敏感或 GPU 计算时用。**字面量必须加后缀 `f`**，如 `3.14f`。 |
| `double`  | `System.Double`  | 8 字节      | ~15~17 位        | ±5.0 × 10⁻³²⁴ ~ ±1.7 × 10³⁰⁸ | **默认的小数类型**（写 `3.14` 编译器默认就是 `double`）。科学计算、物理引擎使用。 |
| `decimal` | `System.Decimal` | **16 字节** | ~28~29 位        | ±1.0 × 10⁻²⁸ ~ ±7.9 × 10²⁸   | **金融和货币专用**。它是十进制浮点数，计算绝对精确（不会出现 0.1+0.2=0.30000000000000004 的问题）。**字面量必须加后缀 `m`**，如 `3.14m`。速度比 `double` 慢，但精度极高。 |

------

### 四、字符与文本类型（您重点关注的 char 和 string）

| 关键字   | .NET 类型       | 类型分类             | 内存大小                         | 本质 / 底层原理                                              |
| :------- | :-------------- | :------------------- | :------------------------------- | :----------------------------------------------------------- |
| `char`   | `System.Char`   | **值类型 (Struct)**  | **2 字节 (16位)**                | **底层核心是一个无符号整数（范围 0~65535）**。它存储的是 **Unicode（UTF-16）编码表**中的码点（Code Point）。当您将整数强转为 `char` 时，计算机不是把数字变成“数字字符”，而是去 Unicode 全球统一字符集里，查找该编号对应的图形（字形）。例如 `(char)97` 对应字母 `'a'`，因为 `'a'` 的编码正好是 97；`(char)11101` 对应一个特殊符号（0x2B5D）。 |
| `string` | `System.String` | **引用类型 (Class)** | **可变（取决于字符数量和编码）** | **本质是由多个 `char` 组成的连续内存序列，托管在堆（Heap）上**。它具有 **不可变性 (Immutable)**：只要对字符串进行修改（拼接、替换、截取），C# 不会修改原来的内存，而是在堆上创建一个全新的字符串对象，并将变量指向新地址。因此，频繁拼接大量字符串时，应使用 `StringBuilder` 来避免性能开销。 |

**特殊补充**：

- `char` 是值类型，所以它**不能**为 `null`（除非使用 `char?` 可空类型）。
- `string` 是引用类型，所以它**可以**为 `null`（表示未指向任何内存地址）。

------

### 五、逻辑与基类类型

| 关键字    | .NET 类型                | 类型分类     | 本质 / 用途                                                  |
| :-------- | :----------------------- | :----------- | :----------------------------------------------------------- |
| `bool`    | `System.Boolean`         | 值类型       | 只有 `true` 和 `false` 两种状态，用于逻辑判断、条件分支、循环开关。 |
| `object`  | `System.Object`          | **引用类型** | 所有 .NET 类型的终极基类（父类）。任何类型都可以隐式转换为 `object`（即装箱操作）。 |
| `dynamic` | `System.Object` 动态绑定 | 引用类型     | 绕过编译时类型检查，在运行时决定类型。极少使用，仅在与动态语言（如 Python、JavaScript）交互时偶尔用到。 |

------

### 六、数据类型转换的核心规则（笔记必抄）

| 转换方向             | 写法示例                                                | 转换性质与结果说明                                           |
| :------------------- | :------------------------------------------------------ | :----------------------------------------------------------- |
| **隐式转换**         | `int i = 10; long l = i;`                               | **自动发生，安全，不会丢失数据**。只能从**小范围**转向**大范围**（如 `int` → `long`，`float` → `double`）。 |
| **显式转换（强转）** | `long l = 100; int i = (int)l;`                         | **必须手动写括号类型**。从大范围转向小范围可能**溢出**（如 64 位转 32 位可能截断数据）。 |
| **数字 → 字符**      | `char c = (char)97;`                                    | **必须显式强转**。结果是去 Unicode 表里查 97 对应的字形（得到 `'a'`）。如果数字超出 0~65535，会发生溢出异常或截断。 |
| **字符 → 数字**      | `int i = 'A';`                                          | **隐式转换（自动）**。直接取出字符底层的 Unicode 编码数值（`'A'` 是 65）。 |
| **数字 → 字符串**    | `string s = 123.ToString();`                            | **绝不能强转！** 必须调用 `.ToString()` 方法，将数值变成“带有数字形状的普通文本”。 |
| **字符 → 字符串**    | `string s = 'a'.ToString();`                            | 或使用拼接 `"" + 'a'`，将单个字符转为文本。                  |
| **字符串 → 数字**    | `int i = int.Parse("123");` 或 `Convert.ToInt32("123")` | 必须使用 `Parse`、`TryParse` 或 `Convert` 类。如果字符串包含非数字字符（如 "abc"），会抛出异常。 |

------

### 七、实战经验：如何正确选择数据类型？

| 业务场景                           | 必须使用的类型           | 绝对禁止的类型                                               |
| :--------------------------------- | :----------------------- | :----------------------------------------------------------- |
| 循环计数器、数组下标、常规整数     | **`int`**                | 无特殊情况不用 `short` 或 `long`                             |
| 超过 21 亿的计数、大数据主键 ID    | **`long`**               | `int`（会导致溢出）                                          |
| 科学计算、物理模拟、分数、三维坐标 | **`double`**             | `float`（精度不足）或 `decimal`（速度太慢）                  |
| **金额、价格、财务结算、银行利息** | **必须用 `decimal`**     | **绝对不能用 `double` 或 `float`**（会有二进制舍入误差，导致账目不对） |
| 存储文件字节、网络流、图片数据     | **`byte[]`**（字节数组） | -                                                            |
| 表示真假、开关、是否启用           | **`bool`**               | 不要用 `int` 的 0 和 1 来代替布尔值                          |
| 单个字符（如性别 M/F、等级 A/B/C） | **`char`**               | -                                                            |
| 姓名、地址、JSON 文本、用户输入    | **`string`**             | -                                                            |



## 常量、变量

程序运行期间
程序开始到程序结束
☆ 变量：在程序运行期间，可以被改变

变量的声明
数据类型 变量名 = 初值;
e g → eg →eg→ int a = 10;
☆ 变量可以不赋初值（在特殊情况下必须要赋初值）

不赋初值时，当前变量的值是默认值
int/float 默认值是 0
char 默认值 ‘\0’（表示空字符）
☆ 常量：在程序运行期间，不能被改变

常量的声明
const 数据类型 变量名 = 初值;
变量名大写（潜规则）
⌨ const float money = 100.35f;
常数必须要赋初值
浮点型声明时注意：
float flo = 1.11f;【float数字后面要加f】
double damage = 1.11d【double后面要加d】
decimal damage = 1.223m【decimal后面要加m】
字符型声明注意：
字符类型(一定要用单引号括起来)
char cha = ‘name’;
常量及变量的命名规则
只能由字母、数字、@和下划线(_)这些组成
数字不能开头，⌨ 1a❌、3f❌、xiaoming1✅
@符号，要么不用，要用必须放在首位，🌰@zhansan✅,zhang@san❌
不能与系统的关键词同名，⌨ int，long，sbyte❌
变量名不能重复
中文变量名语法上是可以的，但极为不推荐
☆ 常量及变量的命名规范

全是英文单词，不要用拼音
驼峰命名法
大驼峰（每个单词的首字母大写，其余字母小写）
1、MyHeroDamage、HeroAttack

小驼峰：（第一个单词首字母不大写，后面每个单词的首字母大写，其余字母小写）
2、myHeroDamage、heroAttack【目前阶段都用小驼峰】

见名知意
yyds（你晓得这是啥？评论区见）❌

## 运算符

☆ 赋值运算符 “=”，是一个运算，将后面的结果赋给前面的变量或常量

前面 = 后面；后面的值赋给前面
前面必须是个变量【不能是具体是数值（2，3，’A‘,“123”）】
后面可以是具体的数值，也可以是变量，也可以是常量
⌨ 算术运算符
+、- 加减法
*、/ 乘除法
a / b 运算之后得到一个结果
被除数 ÷ 除数 = 商
除法有一个禁忌：除数不能为0
% 取余（做除法取余数）
5 % 3 ： 5除以3，得到的余数，即结果
上面的+、-、*、/、%都是二元运算符
++运算符 和 - -运算符
举例：a++; 等价于 a = a+1;
++、- -是一元运算符

### 小测试逐步推演（C# 规则）

表达式：`num++ + --num - ++num`，初始 `num = 10`。

C# 规定运算数按**从左到右**的顺序求值，我们一步步拆解：

1. **计算第一个 `num++`（后置自增）**：
   - 表达式的值是自增**前**的值：`10`
   - 副作用：`num` 变为 `11`
2. **计算 `--num`（前置自减）**：
   - 此时 `num` 是 `11`
   - 先自减：`num` 变为 `10`
   - 表达式的值是自减后的值：`10`
3. **计算第一次加法 `num++ + --num`**：
   - `10 + 10 = 20`（中间结果）
4. **计算最后的 `++num`（前置自增）**：
   - 此时 `num` 还是 `10`（上一步自减后的值）
   - 先自增：`num` 变为 `11`
   - 表达式的值是自增后的值：`11`
5. **计算最终减法 `20 - 11`**：
   - 结果为 `9`
6. **赋值给 `num`**：
   - 最终 `num = 9`

------

### 一个重要的警告 🚨

虽然 C# 算出了 `9`，但**请绝对不要在真实的项目代码中这样写**！

- **可读性极差**：任何维护代码的人（包括未来的你）都会很痛苦。
- **极易出错**：哪怕是稍微改动一下运算符顺序，结果就可能天差地别。

**结论**：面试或考试时，答案是 **`9`**（针对 C#/Java）；但在实际工作中，这行代码应该被**直接拉黑**。😄

### 复合运算符

- a+=b;等价于a=a+b;
- **a=a+4;** ==> **a+=4;**
- a-=b;等价于a=a-b;
- a*=b;等价于a=a*b;
- a/=b;等价于a=a/b;
- a%=b;等价于a=a%b;

## 输入与输出

#### 输出

```c#
Console.WriteLine();
```

输出内容，并换行

```c#
Console.Write();
```

输出内容，不换行

#### 输入

```
Console.Read();
```

从屏幕读取一个字符，并返回该字符所对应的整型数字

```
Console.ReadLine();
```

从屏幕读取一串字符，并返回该字符串

字符串是一个数据类型

1、关键词String
2、表示一串字符
3、用双引号括起来
4、字符串相加可以得到两个字符串组合到一起的字符串

## 预编译执行 Region

- 作用：代码 分成一个区域，方便折叠和展开
- 区域首部：#region
- 区域尾部：#endregion

## 类型转换

- 隐式转换

1、将占用字节小的、取值范围小的、精度小的，转换为占用字节大的、取值范围大的、精度高 → 向上转原则

2、不需要任何的修饰符，会自动转换

```C#
//整型
//1    2     4    8
//sbyte short int long
sbyte englishScore = 100;
//sbyte --> int
int myScore = englishScore;
//int --> long
long classScore = myScore;
//int --> float
float newScore = myScore;
//float --> double
double newClassScore = newScore;

```

显式转换（强制转换）

- 将占用字节大的、取值范围大的、精度高，转换为占用字节小的、取值范围小的、精度小的(向下转)
- 需要强制转修饰符，**会有精度的缺失，甚至数据的错误**
- 转换情况：知道这个数字，在小的数据类型的取值范围内

```C#
//强制转换
int damage = 10000;
//int --> sbyte
sbyte health = (sbyte)damage;
//输出结果
Console.WriteLine(health); //16

float mathScore = 90.5f;
//float --> int
int myAllScore = (int)mathScore;
//会把小数点后面的内容全部舍去
Console.WriteLine(myAllScore); //90

```

## C# 类型转换三大核心方法体系总结

### 一、三大方法各司其职（整体框架）

| 方法类别             | 代表方法                                                     | 核心功能（一句话概括）                                       | 它是谁的方法？                                               |
| :------------------- | :----------------------------------------------------------- | :----------------------------------------------------------- | :----------------------------------------------------------- |
| **数据类型.Parse()** | `int.Parse()`、`bool.Parse()`、`float.Parse()`、`char.Parse()` | **专门把 "字符串" 转成 "特定类型"**。                        | 属于**具体数据类型本身**（`int`、`bool`、`float` 等）的静态方法。 |
| **System.Convert**   | `Convert.ToInt32()`、`Convert.ToBoolean()`、`Convert.ToDouble()`、`Convert.ToChar()`、`Convert.ToSingle()` | **万能转换器**，可以把"任何类型"转成"任何其他类型"（尤其是把字符串转成数值）。 | 属于独立的工具类 **`System.Convert`**。                      |
| **.ToString()**      | `num.ToString()`、`flag.ToString()`、`letter.ToString()`     | **把任何类型（任意变量）变成字符串**。                       | 属于**所有类型**的通用方法（因为所有类都继承自 `object`，而 `object` 有 `ToString()` 方法）。 |

------

### 二、详细拆解与底层规则

1. ### 数据类型.Parse() —— 严格的字符串解析

- **运行机制**：它要求输入的**字符串必须完全是目标类型的合法格式**，否则程序会崩溃（抛异常）。
- **示例明细**：
  - `int.Parse("123")` → 结果：整数 `123`。
  - `int.Parse("123.45")` → 报错（因为包含了小数点，不是纯整数格式）。
  - `bool.Parse("true")` → 结果：布尔值 `true`（注意：只能写 `"True"` 或 `"False"`，不区分大小写）。
  - `float.Parse("3.14")` → 结果：单精度浮点数 `3.14f`。
  - `char.Parse("a")` → 结果：字符 `'a'`（注意：括号里必须恰好是 1 个字符，`char.Parse("ab")` 会报错）。

### 2. System.Convert 工具类 —— 宽容的万能转换器

- **运行机制**：它比 `.Parse()` 稍微**宽容一些**，能处理 `null` 空值（`null` 转数值会返回该类型的默认值，比如 `0`），且支持在不同类型之间互转（不仅仅是字符串）。
- **示例明细**：
  - `Convert.ToInt32("123")` → 结果：整数 `123`（和 Parse 一样）。
  - `Convert.ToInt32(null)` → 结果：整数 `0`（Parse 遇到 null 会直接崩溃，但 Convert 不会）。
  - `Convert.ToBoolean("true")` → 结果：`true`。
  - `Convert.ToSingle("3.14")` → 结果：`float` 类型（3.14f）。
  - `Convert.ToChar("A")` → 结果：字符 `'A'`。
  - **不同类互转**：`Convert.ToInt32('A')` → 结果：整数 `65`（取出字符的 ASCII/Unicode 码）。

### 3. .ToString() —— 任何变量变文本

- **运行机制**：这是最安全的方法，**永远不会报错**。任何类型的变量（数字、布尔、字符、甚至自定义对象）都可以点出 `.ToString()` 方法。
- **示例明细**：
  - `int num = 10;` `num.ToString()` → 结果：字符串 `"10"`。
  - `bool flag = true;` `flag.ToString()` → 结果：字符串 `"True"`。
  - `char letter = 'a';` `letter.ToString()` → 结果：字符串 `"a"`。
  - `object obj = null;` 如果对 `null` 调用 `.ToString()`，程序会崩溃（因为空对象没有方法）。此时建议用 `Convert.ToString(obj)` 代替，它会返回空字符串 `""`。

------

### 三、.Parse() 与 System.Convert 的核心区别（面试必考点）

| 对比维度             | `.Parse()` 方法                                              | `System.Convert` 类                                          |
| :------------------- | :----------------------------------------------------------- | :----------------------------------------------------------- |
| **参数类型**         | **参数只能传 `string` 字符串**。                             | 参数可以是**任意类型**（`object`），不仅仅是字符串。         |
| **对 `null` 的处理** | 传入 `null` 会直接抛出 **`ArgumentNullException`**（程序崩溃）。 | 传入 `null` **不会崩溃**，会返回该类型的默认值（如 `int` 返回 `0`，`bool` 返回 `false`）。 |
| **对格式的要求**     | **极其严格**。必须是完美格式（如 `int.Parse(" 123 ")` 带空格也会报错）。 | 相对宽容，能自动去除前后空格（如 `Convert.ToInt32(" 123 ")` 可以正常工作）。 |
| **性能**             | 稍微快一点（因为只专注于字符串）。                           | 稍微慢一点（因为要处理装箱拆箱和各种类型判断）。             |

------

### 四、特殊警示：强制转型 `(char)` 与 `Convert.ToChar()` 不是一回事

请注意：`Convert.ToChar(11101)` 和 `(char)11101` 的结果虽然可能相同（都是去 Unicode 码表查对应的符号），但**底层逻辑完全不同**：

- **`(char)num`（显式强制转换）**：这是**内存级别的直接截断或重新解释**。编译器直接把 `int` 变量里的二进制数据当作 `char` 来看待，不调用任何方法。
- **`Convert.ToChar(num)`**：这是**调用了一个方法**。这个方法内部会做范围检查（检查数字是否在 0~65535 之间），如果超出范围，它会抛出溢出异常，而不会像强转那样截断数据。

------

### 五、终极实战选型速查表（日常写代码直接对照）

| 你的需求场景                                     | 最安全的写法                                                 | 备选写法                                                     |
| :----------------------------------------------- | :----------------------------------------------------------- | :----------------------------------------------------------- |
| **前端输入框传来"123"，想转成数字**              | 强烈建议用 **`int.TryParse()`**（不会崩溃）或 `Convert.ToInt32()`（容错性高）。 | `int.Parse()`（不推荐，因为用户可能输入空格或空值导致程序崩溃）。 |
| **已知字符串格式绝对完美（比如代码内部写死的）** | 用 `int.Parse("123")`（性能最优）。                          | -                                                            |
| **想把变量显示在界面上或存入日志**               | **直接用 `.ToString()`**（例如 `num.ToString()`）。          | -                                                            |
| **想把字符 'A' 转成数字 65**                     | 直接用 **强制转型 `(int)'A'`**（最直接，0 消耗）。           | `Convert.ToInt32('A')`（多了一次方法调用开销）。             |
| **想把数字 97 转成字符 'a'**                     | 直接用 **强制转型 `(char)97`**。                             | `Convert.ToChar(97)`（更安全，会检查溢出）。                 |
| **不确定字符串是不是纯数字，但又不想程序崩溃**   | **必须使用 `int.TryParse("abc", out int result)`**（返回 bool 表示是否成功）。 | 不要用 `Convert` 或 `Parse`，它们会抛异常。                  |

## 关系运算符 & 逻辑运算符

关系运算符：>，<，>=，<=，==，!=
☆ 逻辑运算符：逻辑运算是bool与bool之间的运算 ↓

&：与运算
1、true & true : true
2、true & false : false
3、false & false : false
4、总结：一假则假

|：或运算
1、true | true : true
2、true | false : true
3、false | false : false
4、总结：一真则真

！：非运算
1、! true : false
2、! false : true

&&：短路与运算
1、普通的&与运算，无论第一个条件是真是假，都会继续判断第二条件
2、短路与运算&&，如果判断第一个条件已经是假，则不会继续判断第二个条件

||：短路或运算
1、普通的|与运算，无论第一个条件是真是假，都会继续判断第二条件
2、短路或运算||，如果判断第一个条件已经是真，则不会继续判断第二个条件

短路&&、||
1、优点：第一个条件已经得知整个逻辑运算的结果，就不会去判断第二个条件了 → 节约了运算量

2、缺点：如果判断中带有运算，如果不进行第二个条件的判断，那第二个条件中的运算也不能执行

## 逻辑运算符深度分析笔记（针对你的“优缺点”疑问）

### 一、& 和 && 的本质区别（不仅是短路）

| 运算符 | 名称            | 对 `bool` 的操作                      | 对整数的操作（重要补充）                             |
| :----- | :-------------- | :------------------------------------ | :--------------------------------------------------- |
| `&`    | 逻辑与 / 按位与 | **不管真假，两边都计算**              | 对两个整数进行**二进制按位与**运算（如 `5 & 3 = 1`） |
| `&&`   | 条件与（短路）  | **假则停**，左边为 `false` 则右边不跑 | **不能用于整数**，只能用于 `bool`                    |
| `|`    | 逻辑或 / 按位或 | **不管真假，两边都计算**              | 对两个整数进行**二进制按位或**运算（如 `5 | 3 = 7`） |
| `||`   | 条件或（短路）  | **真则停**，左边为 `true` 则右边不跑  | **不能用于整数**，只能用于 `bool`                    |

> **结论**：在 C# 中，`&` 和 `|` 是“重载”的。写在 `bool` 表达式里是“非短路逻辑运算”，写在整数表达式里是“位运算”。你笔记中把它们归为逻辑运算（bool与bool）是对的，但需注意它们还有另一重身份。

------

### 二、针对你总结的“缺点”进行真实代码推演

你写的缺点非常精准：**“如果第二个条件中带有运算，短路会导致该运算不执行”**。这在笔试和实际 Bug 排查中极其常见。

#### 场景复现（必须看懂的示例）：

csharp

```
int i = 10;
int j = 5;

// 使用短路与 &&
bool result1 = (i > 20) && (++j > 0);
Console.WriteLine(j); // 输出：5  （因为 i > 20 是 false，++j 根本没执行！）

// 使用普通与 &
bool result2 = (i > 20) & (++j > 0);
Console.WriteLine(j); // 输出：6  （因为 & 两边都算，++j 执行了！）
```



**分析**：

- 在 `&&` 版本中，程序员本意可能是想“判断条件的同时顺便把 `j` 加 1”，但短路直接跳过了 `++j`，导致业务逻辑出错（`j` 没变）。
- 这就是你笔记中写的 **“第二个条件中的运算不能执行”** 的真实含义。

------

### 三、短路运算符的“优点”到底好在哪？（不仅仅是节约 CPU）

你写的是“节约运算量”，但在实际工程中，`&&` 和 `||` 最大的价值**不是省 CPU（因为判断一次布尔值消耗几乎为 0）**，而是：

**避免程序崩溃（NullReferenceException）**。

这是最经典的用法，请务必记在你的笔记中：

csharp

```C#
// 危险写法（使用普通 &）
if (user != null & user.Age > 18) 
{ 
    // 如果 user 是 null，& 会计算右边 user.Age，直接抛出 NullReferenceException 崩溃！
}

// 安全写法（使用短路 &&）
if (user != null && user.Age > 18) 
{ 
    // 左边 user != null 是 false，右边 user.Age 被短路跳过，程序安全！
}
```



**结论**：在 C# 日常编码规范中，**99% 的情况下都应该使用 `&&` 和 `||`**，只有在你**必须**让右侧代码执行副作用（如修改变量、调用方法）时，才考虑使用 `&` 和 `|`（但这种情况极罕见，且通常被认为是“坏味道”代码，不推荐写）。

------

### 四、优先级速查（常见陷阱）

当 `&&`、`||` 和 `!`（非）同时出现时，优先级顺序是：

1. **`!`**（非）—— 最高优先级
2. **`&&`**（短路与）—— 中等
3. **`||`**（短路或）—— 最低

**示例分析**：

csharp

```
bool result = true || false && false;
// 因为 && 优先级高于 ||，所以实际运算为： true || (false && false) => true || false => true
// 而不是 (true || false) && false (结果会是 false)
```



------

### 五、关系运算符（`==`、`!=`）与逻辑运算符连用的注意事项`

- 关系运算符的优先级**低于**算术运算符，但**高于**逻辑运算符。
- 所以 `num + 3 > 5` 会先算 `num + 3`，再比较 `>`。
- 特别警示：**`==` 和 `!=` 对于 `string` 类型比较的是内容（值），而对于自定义类默认比较的是内存地址（引用）**。这一点虽然属于“相等性”范畴，但在写复杂逻辑表达式时极易踩坑，建议旁注在笔记里。



### 对于自定义类默认比较的是内存地址（引用）的解释

是：这句话的意思是：**当你在 C# 中比较两个自定义类的对象（比如 `Person`、`Student` 等）时，如果你没有主动重写（Override）比较规则，C# 默认**不会去比较对象里面的内容**（比如姓名、年龄），而是比较**这两个变量存放的“门牌号”是否相同**。

------

#### 1. 先搞清楚内存地址（引用）是什么

当你 `new` 一个对象时：

csharp

```
Person p1 = new Person();
```



计算机在堆内存（Heap）中划出一块空地，把 `new Person()` 的数据放进去。这块空地的**首地址（门牌号）**，就是所谓的**“内存地址（引用）”**。
变量 `p1` 里面存的**不是**这个人本身，而是这个**门牌号**。

------

#### 2. 直接上代码例子（帮你彻底看懂）

csharp

```
// 1. 定义一个简单的自定义类（没有重写任何比较方法）
public class Person
{
    public string Name { get; set; }
    public int Age { get; set; }
}

// 2. 主程序执行
class Program
{
    static void Main(string[] args)
    {
        // 创建第一个对象
        Person p1 = new Person();
        p1.Name = "张三";
        p1.Age = 20;

        // 创建第二个对象
        Person p2 = new Person();
        p2.Name = "张三";
        p2.Age = 20;

        // 创建第三个对象，指向第一个对象的地址
        Person p3 = p1;

        // 比较 p1 和 p2（内容完全一样，但默认 == 比较的是地址）
        bool result1 = (p1 == p2);
        Console.WriteLine(result1); // 输出：False

        // 比较 p1 和 p3（它们指向同一个地址）
        bool result2 = (p1 == p3);
        Console.WriteLine(result2); // 输出：True

        // 对比：使用 Equals 方法（默认也是比较地址，除非重写）
        bool result3 = p1.Equals(p2);
        Console.WriteLine(result3); // 输出：False
    }
}
```



------

#### 3. 逐步分析（务必对照着看）

| 操作                        | 内存图解                                                     | 比较结果                                      |
| :-------------------------- | :----------------------------------------------------------- | :-------------------------------------------- |
| `Person p1 = new Person();` | 在堆里开辟了 **一号房间**，门牌号是 `0x001`。 `p1` 存着 `0x001`。 | -                                             |
| `Person p2 = new Person();` | 又在堆里开辟了 **二号房间**（哪怕长得一模一样，也是新房间），门牌号是 `0x002`。 `p2` 存着 `0x002`。 | -                                             |
| `p1 == p2`                  | 比较的是 `0x001` 和 `0x002`。                                | **False**（因为地址不同，根本不是同一个房间） |
| `Person p3 = p1;`           | 把 `p1` 里的门牌号 `0x001` 复制给了 `p3`。 现在 `p1` 和 `p3` 都拿着 `0x001`。 | -                                             |
| `p1 == p3`                  | 比较的是 `0x001` 和 `0x001`。                                | **True**（因为地址相同，确实是同一个房间）    |

------

#### 4. 这和 `string` 类型有什么区别？（联系之前的笔记）

你之前记过 `string` 是引用类型（类），但你发现用 `string` 比较内容却好使：

csharp

```
string s1 = "abc";
string s2 = "abc";
Console.WriteLine(s1 == s2); // 输出 True
```



**为什么 string 是类，却能比内容？**
因为微软在 `string` 这个类里面，**早就帮我们重写了 `==` 和 `Equals()` 方法**，让它改为去比较里面的字符文本，而不是比较门牌号。

------

#### 5. 如果你想比较自定义类的“内容”，该怎么办？（笔记必抄）

如果你想让 `Person` 也像 `string` 一样，比内容（比如姓名和年龄都一样就算相等），你必须在类里面**重写（Override）`Equals` 和 `GetHashCode` 方法**，并且在可能的情况下重载 `==` 运算符。

代码示例（标准写法）：

csharp

```
public class Person
{
    public string Name { get; set; }
    public int Age { get; set; }

    // 重写 Equals 方法，制定“内容相等”的规则
    public override bool Equals(object obj)
    {
        if (obj is Person other)
        {
            return this.Name == other.Name && this.Age == other.Age;
        }
        return false;
    }

    // 只要重写 Equals，必须同时重写 GetHashCode（这是 C# 强制性要求）
    public override int GetHashCode()
    {
        return HashCode.Combine(Name, Age);
    }
}

// 此时再执行 Person p1 = new Person{Name="张三", Age=20}; Person p2 = new Person{Name="张三", Age=20};
// Console.WriteLine(p1.Equals(p2)); 输出：True
// 但 Console.WriteLine(p1 == p2); 默认还是 False（除非你再重载 == 运算符）
```



------

#### 一句话总结

- **默认情况（没重写）**：自定义类比较 `==` 和 `Equals`，就是在问 **“你俩是不是住在同一个门牌号（同一块内存）”**。
- **特殊重写后（如 string）**：才变成了问 **“你俩长得（内容）是不是一模一样”**。
- **结论**：对于自定义类，如果你直接写 `if (p1 == p2)`，绝大多数情况下你**是在比较是不是同一个对象**，而不是比较内容！记死了，这是 C# 新手的头号踩坑点。😊
  </thinking>这句话的意思是：**当你在 C# 中比较两个自定义类的对象（比如 `Person`、`Student` 等）时，如果你没有主动重写（Override）比较规则，C# 默认不会去比较对象里面的内容（比如姓名、年龄），而是比较这两个变量存放的“门牌号”是否相同。**

好的，这是对你提供的**条件判断**和**循环语句**内容的完整总结笔记，已按你的原文结构整理，可以直接抄录。

---

## C# 条件判断与循环语句完整笔记

### 一、条件运算符（条件判断）

#### 1.1 三元运算符（条件运算符）

**语法格式**：
```csharp
条件表达式 ? 结果a : 结果b
```

**执行逻辑**：
- 先计算 `条件表达式` 的值
- 如果为 `true`，返回 `结果a`
- 如果为 `false`，返回 `结果b`

**代码示例**：
```csharp
int a = 50;
int b = 0;  // 需要先初始化，否则编译报错

b = a > 500 ? a : b;  // 条件为 false，返回 b（0），所以 b = 0
// 如果 a > 500，则 b = a；否则 b 保持原值
```

**注意事项**：
- 三元运算符必须要有返回值，不能只执行语句不返回
- `结果a` 和 `结果b` 的数据类型必须一致，或者可以隐式转换
- 适用于简单的条件赋值，逻辑复杂时应使用 `if-else`

---

#### 1.2 if 语句（三种形式）

##### 第一种形式：单分支

**语法格式**：
```csharp
if (条件表达式)
{
    语句1;
}
```

**执行逻辑**：
- 条件为 `true` 时，执行 `语句1`
- 条件为 `false` 时，跳过 `语句1`，继续往下执行

**代码示例**：
```csharp
int age = 18;
if (age >= 18)
{
    Console.WriteLine("已成年");
}
```

---

##### 第二种形式：双分支（if-else）

**语法格式**：
```csharp
if (条件表达式)
{
    语句1;
}
else
{
    语句2;
}
```

**执行逻辑**：
- 条件为 `true` 时，执行 `语句1`
- 条件为 `false` 时，执行 `语句2`

**代码示例**：
```csharp
int score = 85;
if (score >= 60)
{
    Console.WriteLine("及格");
}
else
{
    Console.WriteLine("不及格");
}
```

---

##### 第三种形式：多分支（if-else if-else）

**语法格式**：
```csharp
if (条件表达式1)
{
    语句1;
}
else if (条件表达式2)
{
    语句2;
}
else
{
    语句3;
}
```

**执行逻辑**：
- 依次判断条件，从上到下
- 遇到第一个为 `true` 的条件，执行对应的语句块，然后跳出整个结构
- 如果所有条件都为 `false`，执行 `else` 中的 `语句3`

**代码示例**：
```csharp
int score = 85;
if (score >= 90)
{
    Console.WriteLine("优秀");
}
else if (score >= 80)
{
    Console.WriteLine("良好");
}
else if (score >= 60)
{
    Console.WriteLine("及格");
}
else
{
    Console.WriteLine("不及格");
}
// 输出：良好
```

**注意事项**：
- `else if` 可以写任意多个
- `else` 是可选的，可以省略
- 条件判断是**互斥**的，只会执行第一个匹配的分支

---

#### 1.3 switch 语句

**语法格式**：
```csharp
switch (表达式)
{
    case 值1:
        语句1;
        break;
    case 值2:
        语句2;
        break;
    // ... 可以有任意多个 case
    default:
        默认语句;
        break;
}
```

**执行逻辑**：
- 计算 `表达式` 的值
- 从上到下依次与每个 `case` 后面的值进行匹配
- 找到匹配的值后，执行对应的语句，遇到 `break` 跳出整个 `switch`
- 如果没有匹配的 `case`，执行 `default` 中的语句

**代码示例**：
```csharp
int day = 4;
switch (day)
{
    case 1:
        Console.WriteLine("Monday");
        break;
    case 2:
        Console.WriteLine("Tuesday");
        break;
    case 3:
        Console.WriteLine("Wednesday");
        break;
    case 4:
        Console.WriteLine("Thursday");
        break;
    case 5:
        Console.WriteLine("Friday");
        break;
    case 6:
        Console.WriteLine("Saturday");
        break;
    case 7:
        Console.WriteLine("Sunday");
        break;
    default:
        Console.WriteLine("无效的日期");
        break;
}
// 输出：Thursday
```

**重要规则**：

1. **`break` 不能省略**：除了极少数特殊情况（如下一条），每个 `case` 末尾必须加 `break`，否则编译报错。

2. **空 `case` 可以不加 `break`**：如果 `case` 冒号后面没有任何语句（即为空），可以不加 `break`，它会自动"穿透"到下一个 `case`。
   ```csharp
   switch (grade)
   {
       case 'A':
       case 'B':
           Console.WriteLine("成绩良好");
           break;
       case 'C':
           Console.WriteLine("成绩一般");
           break;
   }
   // 当 grade = 'A' 或 'B' 时，都输出"成绩良好"
   ```

3. **`switch` 支持的数据类型**：
   - 整数类型（`int`、`long`、`byte` 等）
   - 字符类型（`char`）
   - 字符串类型（`string`）
   - 布尔类型（`bool`）
   - 枚举类型（`enum`）

4. **⚠️ 浮点数与 `switch` 的注意事项**：
   - `switch` 的括号中**允许**使用浮点型变量（`float`、`double`）
   - 但**强烈不推荐**这样做！
   - 原因：浮点数在计算机中存储时有精度误差（如 `0.1 + 0.2 != 0.3`），直接比较相等性很容易失败
   - **企业面试题常考**：判断一个浮点数是否等于 5，正确做法是比较差值是否小于一个极小值（如 `1e-10`），而不是直接用 `==`

   ```csharp
   // ❌ 错误写法：浮点数直接判等
   double num = 0.1 + 0.2;
   if (num == 0.3)  // 大概率是 false！
   
   // ✅ 正确写法：比较差值
   if (Math.Abs(num - 0.3) < 1e-10)  // true
   ```

---

### 二、循环语句

#### 2.1 while 循环

**语法格式**：
```csharp
while (条件表达式)
{
    // 循环体
}
```

**执行逻辑**：
1. 先判断 `条件表达式`
2. 如果为 `true`，执行循环体
3. 执行完后再次判断条件
4. 如果为 `false`，跳出循环，继续执行后面的代码

**代码示例**：
```csharp
int i = 1;
while (i <= 5)
{
    Console.WriteLine(i);
    i++;
}
// 输出：1 2 3 4 5
```

**注意事项**：
- 循环体中必须有改变条件变量的语句，否则会形成**死循环**
- 如果条件一开始就是 `false`，循环体一次都不会执行

---

#### 2.2 break 关键字

**作用**：**跳出本层循环**（终止当前所在的整个循环）

**使用场景**：通常与 `if` 条件判断连用，满足某个条件时提前终止循环

**代码示例**：
```csharp
int i = 1;
while (i <= 10)
{
    if (i == 5)
    {
        break;  // i == 5 时跳出循环
    }
    Console.WriteLine(i);
    i++;
}
// 输出：1 2 3 4
```

---

#### 2.3 continue 关键字

**作用**：**结束本次循环**，跳过 `continue` 后面剩余的代码，立即进入下一次循环判断

**使用场景**：通常与 `if` 条件判断连用，满足某个条件时跳过当前迭代

**代码示例**：
```csharp
int i = 0;
while (i < 5)
{
    i++;
    if (i == 3)
    {
        continue;  // i == 3 时跳过输出，直接进入下一次循环
    }
    Console.WriteLine(i);
}
// 输出：1 2 4 5
```

---

#### 2.4 break 与 continue 的区别（重点对比）

| 关键字     | 作用             | 效果                                           |
| :--------- | :--------------- | :--------------------------------------------- |
| `break`    | **跳出本层循环** | 整个循环立即终止，不再执行任何迭代             |
| `continue` | **结束本次循环** | 当前迭代终止，跳过后续代码，进入下一次循环判断 |

---

### 三、速查表（建议截图保存）

| 语法结构          | 关键词                            | 适用场景             |
| :---------------- | :-------------------------------- | :------------------- |
| 三元运算符 `? :`  | `?` `:`                           | 简单的二选一赋值     |
| `if`              | `if`                              | 单一条件判断         |
| `if-else`         | `if` `else`                       | 二选一分支           |
| `if-else if-else` | `if` `else if` `else`             | 多条件互斥选择       |
| `switch-case`     | `switch` `case` `default` `break` | 多值匹配（等值判断） |
| `while`           | `while`                           | 条件型循环           |
| `break`           | `break`                           | 提前终止循环         |
| `continue`        | `continue`                        | 跳过本次循环         |

---

**最终提醒**：
- 浮点数**不要**用 `==` 或 `switch` 做相等判断，用差值比较法
- `switch` 中每个 `case` 末尾必须有 `break`（空 `case` 除外）
- `break` 跳出整个循环，`continue` 只跳过本次循环——**这两个不要搞混** 😊

好的，这是对你提供的**数组**内容的完整总结和补充笔记。你原文中的内容基本正确，我会帮你**理顺逻辑、补充细节、加粗重点**，方便你直接抄录。

---

## C# 数组完整笔记

### 一、什么是数组？

**数组** 是一种**引用类型**，用于存储**相同数据类型**的一组有序数据的集合。

- 数组中的每个数据称为**元素**
- 每个元素通过**下标（索引）** 来访问
- 下标从 **0** 开始计数

---

### 二、一维数组的初始化（三种方式）

#### 方式一：动态初始化（指定长度）

```csharp
数据类型[] 数组名 = new 数据类型[数组长度];
```

**特点**：
- 只指定了数组长度，没有赋初值
- 系统会自动为每个元素赋予**默认值**

**各类型默认值表**：

| 数据类型 | 默认值  | 说明                                                    |
| :------- | :------ | :------------------------------------------------------ |
| `int`    | `0`     | 整数默认 0                                              |
| `float`  | `0`     | 浮点数默认 0                                            |
| `double` | `0`     | 浮点数默认 0                                            |
| `bool`   | `false` | 布尔默认 false                                          |
| `char`   | `'\0'`  | 空字符（不是空格，是 ASCII 码 0）                       |
| `string` | `null`  | **注意**：string 是引用类型，默认值是 `null`，不是 `""` |

**代码示例**：
```csharp
int[] arr = new int[5];   // 长度为5，每个元素初始为0
// arr = {0, 0, 0, 0, 0}
```

---

#### 方式二：动态初始化（指定长度 + 赋值）

```csharp
数据类型[] 数组名 = new 数据类型[数组长度] { 元素1, 元素2, ... };
```

**特点**：
- 同时指定长度和初始值
- **长度必须与元素个数一致**，否则编译报错

**代码示例**：
```csharp
int[] arr = new int[3] { 10, 20, 30 };
```

---

#### 方式三：动态初始化（省略长度，直接赋值）

```csharp
数据类型[] 数组名 = new 数据类型[] { 元素1, 元素2, ... };
```

**特点**：
- 省略了数组长度，系统根据元素个数自动推断
- 代码更简洁

**代码示例**：
```csharp
int[] arr = new int[] { 10, 20, 30, 40 };
```

---

#### 方式四：静态初始化（最简洁的写法）⭐

```csharp
数据类型[] 数组名 = { 元素1, 元素2, ... };
```

**特点**：
- 这是**语法糖**（简化写法），编译器自动推断类型和长度
- 最常用的写法

**代码示例**：
```csharp
int[] arr = { 10, 20, 30, 40, 50 };
```

---

### 三、数组的访问

#### 3.1 通过下标（索引）访问

```csharp
数组名[下标]   // 下标从 0 开始
```

**代码示例**：
```csharp
int[] arr = { 10, 20, 30, 40, 50 };
Console.WriteLine(arr[0]);  // 输出 10
Console.WriteLine(arr[2]);  // 输出 30
arr[1] = 100;               // 修改第二个元素为 100
```

---

#### 3.2 数组长度（`Length` 属性）

```csharp
int length = 数组名.Length;
```

**特点**：
- `Length` 是数组的一个**只读属性**，只能获取，不能修改
- 返回数组中元素的**总个数**

**代码示例**：
```csharp
int[] arr = { 10, 20, 30, 40, 50 };
Console.WriteLine(arr.Length);  // 输出 5
```

---

#### 3.3 ⚠️ 数组越界异常（IndexOutOfRangeException）

**访问数组时，下标必须在 `0 ~ Length - 1` 范围内，否则程序会崩溃！**

```csharp
int[] arr = { 10, 20, 30 };
Console.WriteLine(arr[3]);  // ❌ 报错：索引超出范围（最大下标是 2）
```

**记住**：最后一个元素的下标是 `Length - 1`，不是 `Length`。

---

### 四、值类型 vs 引用类型（回顾 + 深化）

#### 4.1 值类型

- **存储在栈内存（Stack）** 中
- 栈内存里存的是**具体的值**
- 赋值时是**复制整个数据**

**已学过的值类型**：
- `int`、`float`、`double`、`bool`、`char`、`byte`、`long`、`short` 等

**代码示例**：
```csharp
int a = 10;
int b = a;   // 把 a 的值复制一份给 b
b = 20;      // 修改 b 不影响 a
Console.WriteLine(a);  // 输出 10
```

---

#### 4.2 引用类型

- **存储在堆内存（Heap）** 中
- 栈内存里存的是**堆内存的地址（引用）**，而不是数据本身
- 赋值时是**复制地址**，多个变量可能指向同一个对象

**已学过的引用类型**：
- `string`（特殊的引用类型）
- **数组**（包括一维、多维、锯齿数组）
- 自定义类（`class`）

**代码示例**（验证数组是引用类型）：
```csharp
int[] arr1 = { 1, 2, 3 };
int[] arr2 = arr1;        // 把 arr1 的地址复制给 arr2，它们指向同一个数组
arr2[0] = 100;            // 修改 arr2 会同时影响 arr1
Console.WriteLine(arr1[0]);  // 输出 100
```

---

#### 4.3 值类型与引用类型对比表

| 对比维度     | 值类型                                   | 引用类型                             |
| :----------- | :--------------------------------------- | :----------------------------------- |
| **存储位置** | 栈（Stack）                              | 堆（Heap）                           |
| **存储内容** | 数据本身                                 | 数据的地址（引用）                   |
| **赋值行为** | 复制整个数据                             | 复制地址（指向同一对象）             |
| **默认值**   | 数字为 0，bool 为 false，char 为 '\0'    | `null`                               |
| **代表类型** | `int`、`float`、`bool`、`char`、`struct` | `string`、`array`、`class`、`object` |

---

### 五、遍历数组

**遍历**：按顺序访问数组中的每一个元素。

#### 5.1 使用 `for` 循环遍历（最常用）2222222222222\\

```csharp
int[] arr = { 10, 20, 30, 40, 50 };
for (int i = 0; i < arr.Length; i++)
{
    Console.WriteLine(arr[i]);
}
// 输出：10 20 30 40 50
```

---

#### 5.2 使用 `foreach` 循环遍历（更简洁）

```csharp
int[] arr = { 10, 20, 30, 40, 50 };
foreach (int item in arr)
{
    Console.WriteLine(item);
}
// 输出：10 20 30 40 50
```

**`for` vs `foreach` 区别**：

| 对比维度         | `for` 循环                   | `foreach` 循环                  |
| :--------------- | :--------------------------- | :------------------------------ |
| **是否知道下标** | 有下标 `i`，可以获取当前位置 | **没有下标**，只能取到元素值    |
| **能否修改元素** | 可以（通过 `arr[i] = 新值`） | **不能修改**（`item` 是只读的） |
| **适用场景**     | 需要下标或修改元素时         | 只读取元素值时                  |

---

### 六、注意事项汇总

1. **数组长度一旦确定，不可改变**（不能动态扩容）
2. **下标从 0 开始**，最大下标是 `Length - 1`
3. **访问越界会抛出异常**，程序崩溃
4. **数组是引用类型**，赋值传的是地址
5. `string` 类型的数组默认值是 `null`，不是 `""`
6. `char` 的默认值是 `'\0'`（空字符），不是空格 `' '`

---

### 七、速查表

| 操作                     | 写法                              | 说明             |
| :----------------------- | :-------------------------------- | :--------------- |
| 声明数组                 | `int[] arr;`                      | 仅声明，未初始化 |
| 动态初始化（指定长度）   | `int[] arr = new int[5];`         | 所有元素为默认值 |
| 动态初始化（长度+赋值）  | `int[] arr = new int[3] {1,2,3};` | 长度必须匹配     |
| 动态初始化（省略长度）   | `int[] arr = new int[] {1,2,3};`  | 系统自动推断长度 |
| **静态初始化（最常用）** | `int[] arr = {1,2,3};`            | 语法糖，简洁     |
| 获取长度                 | `arr.Length`                      | 只读属性         |
| 访问元素                 | `arr[0]`                          | 下标从 0 开始    |
| for 循环遍历             | `for(int i=0; i<arr.Length; i++)` | 有下标，可修改   |
| foreach 循环遍历         | `foreach(int x in arr)`           | 无下标，只读     |

---

**最终提醒**：

- 数组下标从 **0** 开始，**不是从 1 开始**，这是新手最常犯的错误！
- `arr.Length` 是元素个数，最后一个元素是 `arr[Length - 1]`
- 数组是引用类型，赋值时是传地址，不是复制数据
- 遍历数组优先用 `for`（需要下标）或 `foreach`（只读场景）😊

好的，这是对你提供的**二维数组、结构体、枚举**内容的完整总结笔记，已按原文结构整理并补充了必要的细节说明。

---

## C# 二维数组、结构体、枚举完整笔记

### 一、冒泡排序

#### 1.1 核心思想

**冒泡排序**是一种简单的排序算法，核心逻辑是：

- **相邻元素两两比较**，如果前大后小（升序排列），则交换位置
- **每轮确定一个最大值**，放到数组的末尾
- 经过 `n-1` 轮后，整个数组完成排序

**降序排列**：前小后大则交换，每轮确定最小值放到末尾

---

#### 1.2 冒泡排序优化版代码（带标志位）

```csharp
int[] array = { 5, 3, 8, 1, 9, 2 };

for (int i = 0; i < array.Length; i++)
{
    // 标志位：记录本轮是否发生过交换
    bool IsjiaoHuan = false;

    // 内层循环：每轮比较次数递减
    // array.Length - i - 1：因为每轮末尾已排好的元素不用再比较
    for (int j = 0; j < array.Length - i - 1; j++)
    {
        if (array[j] > array[j + 1])   // 前大后小 → 交换（升序）
        {
            IsjiaoHuan = true;
            // 交换两个元素
            int temp = array[j];
            array[j] = array[j + 1];
            array[j + 1] = temp;
        }
    }

    // 优化：如果本轮没有发生交换，说明数组已经有序，提前结束
    if (!IsjiaoHuan)
    {
        break;
    }

    // 打印每轮排序后的结果（调试用）
    for (int m = 0; m < array.Length; m++)
    {
        Console.Write(array[m] + "\t");
    }
    Console.WriteLine();
}

Console.WriteLine("排序结束");
for (int m = 0; m < array.Length; m++)
{
    Console.Write(array[m] + "\t");
}
```

---

#### 1.3 冒泡排序图示（升序）

```
原始数组：  [5, 3, 8, 1, 9, 2]

第1轮： 3, 5, 1, 8, 2, 9    ← 最大值 9 排到最后
第2轮： 3, 1, 5, 2, 8, 9    ← 最大值 8 排到倒数第二
第3轮： 1, 3, 2, 5, 8, 9    ← 最大值 5 排到倒数第三
第4轮： 1, 2, 3, 5, 8, 9    ← 最大值 3 排到倒数第四（已完成排序）
第5轮： 没有发生交换 → 提前结束
```

---

#### 1.4 时间复杂度

| 情况                 | 时间复杂度                 |
| :------------------- | :------------------------- |
| 最坏情况（完全逆序） | O(n²)                      |
| 最好情况（已经有序） | O(n)（优化版只需遍历一轮） |
| 平均情况             | O(n²)                      |

---

### 二、二维数组

#### 2.1 二维数组的定义

```csharp
数据类型[,] 数组名;
```

**示例**：
```csharp
int[,] array;   // 声明一个二维数组，未初始化
```

---

#### 2.2 二维数组的初始化

##### 方式一：动态初始化（指定长度）

```csharp
数据类型[,] 数组名 = new 数据类型[第一维长度, 第二维长度];
```

**示例**：
```csharp
int[,] arr = new int[50, 3];   // 50行，3列，所有元素默认为0
```

---

##### 方式二：动态初始化（指定长度 + 赋值）

```csharp
数据类型[,] 数组名 = new 数据类型[,] { {值1, 值2}, {值3, 值4} };
```

**示例**：
```csharp
int[,] arr = new int[,] { {1, 0, 1}, {3, 0, 5} };   // 2行3列
```

---

##### 方式三：静态初始化（最简洁）

```csharp
数据类型[,] 数组名 = { {值1, 值2}, {值3, 值4} };
```

**示例**：
```csharp
int[,] arr = { {1, 0, 1}, {3, 0, 5} };   // 2行3列
```

---

#### 2.3 二维数组的元素访问

```csharp
数组名[第一维下标, 第二维下标]
```

**示例**：
```csharp
int[,] arr = { {1, 0, 1}, {3, 0, 5} };
Console.WriteLine(arr[0, 0]);   // 输出：1（第1行第1列）
Console.WriteLine(arr[1, 2]);   // 输出：5（第2行第3列）
arr[1, 1] = 100;                // 修改第2行第2列为100
```

**⚠️ 谨防数组越界**：
- 第一维下标范围：`0 ~ GetLength(0) - 1`
- 第二维下标范围：`0 ~ GetLength(1) - 1`

---

#### 2.4 二维数组的长度属性

| 属性/方法            | 返回值     | 说明                                        |
| :------------------- | :--------- | :------------------------------------------ |
| `array.Length`       | 总元素个数 | 返回二维数组中所有元素的总数（行数 × 列数） |
| `array.GetLength(0)` | 第一维长度 | 返回行数                                    |
| `array.GetLength(1)` | 第二维长度 | 返回列数                                    |

**示例**：
```csharp
int[,] arr = { {1, 0, 1}, {3, 0, 5} };
Console.WriteLine(arr.Length);        // 输出：6（2行 × 3列）
Console.WriteLine(arr.GetLength(0));  // 输出：2（行数）
Console.WriteLine(arr.GetLength(1));  // 输出：3（列数）
```

---

#### 2.5 二维数组的遍历

方式一：for 循环遍历（推荐）

```csharp
int[,] heroData = { {1, 0, 1}, {3, 0, 5} };

for (int i = 0; i < heroData.GetLength(0); i++)      // 遍历行
{
    for (int j = 0; j < heroData.GetLength(1); j++)  // 遍历列
    {
        Console.Write(heroData[i, j] + "\t");
    }
    Console.WriteLine();   // 每行结束后换行
}
```

**输出结果**：
```
1	0	1	
3	0	5	
```

---

##### 方式二：foreach 迭代遍历（只读，不能修改）

```csharp
int[,] number = { {1, 2, 3}, {4, 5, 6} };

foreach (var item in number)
{
    Console.WriteLine(item);
    // ⚠️ 迭代遍历是只读的，不能修改元素
    // item = 1;   // ❌ 编译报错
}
```

**foreach vs for 对比**：

| 对比维度           | `for` 循环             | `foreach` 循环           |
| :----------------- | :--------------------- | :----------------------- |
| **是否可修改元素** | ✅ 可以（通过下标赋值） | ❌ 不可以（只读迭代变量） |
| **是否有下标**     | ✅ 有（i, j）           | ❌ 没有                   |
| **性能**           | 更快                   | 稍慢（有额外开销）       |
| **推荐场景**       | 需要修改或需要下标时   | 仅读取元素时             |

> **建议**：能用 `for` 尽量用 `for`，`foreach` 性能消耗略大。

---

### 三、枚举类型（Enum）

#### 3.1 什么是枚举？

- 枚举是一个**自定义类型**
- 枚举是一个**值类型**（继承自 `System.Enum`）
- 枚举用于表示一组**相关的命名常量**，使代码更易读

---

#### 3.2 枚举的创建

**语法格式**：
```csharp
enum 枚举名
{
    枚举项1,
    枚举项2,
    枚举项3
}
```

**示例**：
```csharp
// 装备类型枚举
enum EquipType
{
    Helmet = 100,      // 头盔，显式赋值为 100
    BodyArmor = 200,   // 防弹衣，显式赋值为 200
    Knapsack,          // 背包，默认值为 201（自动递增）
    Knife              // 匕首，默认值为 202（自动递增）
}
```

**重要规则**：
- 枚举项的默认值从 0 开始，依次递增
- 可以显式指定某个枚举项的值，后续项默认递增
- 枚举项的值**不要求唯一**，但不推荐重复

---

#### 3.3 枚举类型变量的定义与使用

```csharp
// 定义枚举变量
EquipType myEquip = EquipType.Knapsack;
EquipType yourEquip = EquipType.Knapsack;

// 判断枚举变量
if (myEquip == EquipType.BodyArmor)
{
    Console.WriteLine("装备了防弹衣");
}

// 在 switch 中使用枚举
switch (myEquip)
{
    case EquipType.BodyArmor:
        Console.WriteLine("防弹衣");
        break;
    case EquipType.Helmet:
        Console.WriteLine("头盔");
        break;
    case EquipType.Knife:
        Console.WriteLine("匕首");
        break;
    default:
        Console.WriteLine("其他装备");
        break;
}
```

---

#### 3.4 枚举与整型的相互转换

```csharp
// 枚举 → 整型（强制转换）
EquipType myEquip = EquipType.Knapsack;
int num = (int)myEquip;
Console.WriteLine(num);   // 输出：201

// 整型 → 枚举（强制转换）
myEquip = (EquipType)200;
Console.WriteLine(myEquip);   // 输出：BodyArmor（因为 200 对应 BodyArmor）

// 枚举变量可以参与整数运算
Console.WriteLine(myEquip + 2);   // 输出：202（枚举值 + 2）
```

---

#### 3.5 枚举的常用方法

| 方法               | 作用               | 示例                                                   |
| :----------------- | :----------------- | :----------------------------------------------------- |
| `Enum.Parse()`     | 将字符串转换为枚举 | `(EquipType)Enum.Parse(typeof(EquipType), "Helmet")`   |
| `Enum.GetName()`   | 获取枚举项的名称   | `Enum.GetName(typeof(EquipType), 200)` → `"BodyArmor"` |
| `Enum.GetValues()` | 获取枚举所有值     | `Enum.GetValues(typeof(EquipType))`                    |
| `Enum.IsDefined()` | 检查值是否在枚举中 | `Enum.IsDefined(typeof(EquipType), 999)` → `false`     |

---

### 四、结构体类型（Struct）

#### 4.1 什么是结构体？

- 结构体是一个**自定义类型**
- 结构体是一个**值类型**（继承自 `System.ValueType`）
- 结构体可以包含：字段、属性、方法、构造函数等
- 适合用来封装**轻量级的数据组合**

---

#### 4.2 结构体的创建

**语法格式**：
```csharp
struct 结构体名
{
    public 数据类型 字段名1;
    public 数据类型 字段名2;
    // ...
}
```

**示例**：
```csharp
// 学生类型
struct Student
{
    public string name;   // 姓名
    public char sex;      // 性别
    public int age;       // 年龄
}
```

---

#### 4.3 结构体变量的声明与字段赋值

```csharp
// 声明结构体变量（不需要 new）
Student xiaoming;

// 为字段赋值（必须给所有字段赋值后才能使用）
xiaoming.name = "xiaoming";
xiaoming.age = 16;
xiaoming.sex = 'M';

// 使用结构体
Console.WriteLine($"{xiaoming.name}, {xiaoming.age}岁");
```

**⚠️ 注意**：
- 结构体的**所有字段必须在访问前完成赋值**
- 否则编译报错（使用了未赋值的变量）

---

4.4 结构体的构造函数

#### 默认构造函数（无参）

- **结构体默认自带无参构造函数**，开发者**不能**自己创建无参构造函数
- 无参构造函数会将所有字段初始化为默认值（数值为0，引用为null）

```csharp
public Student()   // ❌ 编译报错！结构体不能显式定义无参构造
{
}
```

---

#### 自定义构造函数（带参）

```csharp
struct Student
{
    public string name;
    public char sex;
    public int age;

    // 自定义构造函数
    public Student(string n, char s, int a)
    {
        // ⚠️ 必须给所有字段赋初值
        name = n;
        sex = s;
        age = a;
    }
}
```

**使用自定义构造函数**：
```csharp
// 使用 new + 自定义构造函数
Student xiaogang = new Student("xiaogang", 'M', 18);
```

---

#### 4.5 new 结构体与不使用 new 的区别

| 方式           | 写法                         | 是否必须赋值                     | 说明                             |
| :------------- | :--------------------------- | :------------------------------- | :------------------------------- |
| **不使用 new** | `Student s;`                 | **必须给所有字段赋值**后才能使用 | 变量在栈上分配                   |
| **使用 new**   | `Student s = new Student();` | 不需要手动赋值，所有字段有默认值 | 调用默认构造函数，字段自动初始化 |

---

#### 4.6 结构体练习题（完整代码）

**需求**：

1. 创建英雄装备结构体，包含：名称、攻击力加成、法术强度加成、血量加成、装备类型（枚举）
2. 有5个装备保存在结构体数组中，找出**血量加成最高**的装备
3. 对装备数组按照**攻击力加成升序排序**并打印

```csharp
// 1. 定义装备类型枚举
enum EquipType
{
    AD,      // 物理攻击
    AP,      // 法术攻击
    HP,      // 生命值
    Other    // 其他
}

// 2. 定义英雄装备结构体
struct HeroEquip
{
    public string name;          // 装备名称
    public float adAddition;     // 攻击力加成
    public float apAddition;     // 法术强度加成
    public float hpAddition;     // 血量加成
    public EquipType equipType;  // 装备类型

    // 自定义构造函数
    public HeroEquip(string name, float adBuff, float apBuff, float hpBuff, EquipType equipType)
    {
        this.name = name;
        adAddition = adBuff;
        apAddition = apBuff;
        hpAddition = hpBuff;
        this.equipType = equipType;
    }
}

// 3. 主程序
class Program
{
    static void Main(string[] args)
    {
        // 创建装备对象
        HeroEquip wjzr = new HeroEquip("无尽之刃", 100, 0, 50, EquipType.AD);
        HeroEquip yxj = new HeroEquip("饮血剑", 80, 0, 20, EquipType.AD);
        HeroEquip ktkj = new HeroEquip("狂徒铠甲", 0, 0, 150, EquipType.HP);
        HeroEquip dmj = new HeroEquip("兰德里的折磨", 20, 100, 0, EquipType.AP);
        HeroEquip wz = new HeroEquip("王者之刃", 60, 30, 80, EquipType.AD);

        // 声明结构体数组
        HeroEquip[] heroEquips = { wjzr, yxj, ktkj, dmj, wz };

        // ===== 任务1：找出血量加成最高的装备 =====
        HeroEquip maxHPEquip = heroEquips[0];

        for (int i = 1; i < heroEquips.Length; i++)
        {
            if (maxHPEquip.hpAddition < heroEquips[i].hpAddition)
            {
                maxHPEquip = heroEquips[i];
            }
        }

        Console.WriteLine("血量加成最高的装备：");
        Console.WriteLine($"名称：{maxHPEquip.name}，血量加成：{maxHPEquip.hpAddition}");
        Console.WriteLine();

        // ===== 任务2：按攻击力加成升序排序（冒泡排序） =====
        for (int i = 0; i < heroEquips.Length - 1; i++)
        {
            bool swapped = false;
            for (int j = 0; j < heroEquips.Length - 1 - i; j++)
            {
                if (heroEquips[j].adAddition > heroEquips[j + 1].adAddition)
                {
                    // 交换两个结构体
                    HeroEquip temp = heroEquips[j];
                    heroEquips[j] = heroEquips[j + 1];
                    heroEquips[j + 1] = temp;
                    swapped = true;
                }
            }
            if (!swapped) break;
        }

        // 打印排序后的结果
        Console.WriteLine("按攻击力加成升序排列：");
        foreach (var equip in heroEquips)
        {
            Console.WriteLine($"名称：{equip.name}，攻击力：{equip.adAddition}，血量：{equip.hpAddition}");
        }

        Console.Read();
    }
}
```

---

### 五、值类型 vs 引用类型（回顾）

| 对比维度     | 值类型                                               | 引用类型                  |
| :----------- | :--------------------------------------------------- | :------------------------ |
| **存储位置** | 栈（Stack）                                          | 堆（Heap）                |
| **存储内容** | 数据本身                                             | 数据的地址（引用）        |
| **赋值行为** | 复制整个数据                                         | 复制地址（指向同一对象）  |
| **默认值**   | 数值为0，bool为false，char为'\0'                     | `null`                    |
| **代表类型** | `int`、`float`、`bool`、`char`、**枚举**、**结构体** | `string`、`数组`、`class` |

**枚举和结构体都是值类型**，这一点需要特别记住！

---

### 六、速查表

| 知识点           | 核心语法                                     |
| :--------------- | :------------------------------------------- |
| 冒泡排序         | 相邻比较，前大后小则交换，每轮确定一个最大值 |
| 二维数组声明     | `int[,] arr;`                                |
| 二维数组初始化   | `int[,] arr = { {1,2}, {3,4} };`             |
| 二维数组访问     | `arr[行下标, 列下标]`                        |
| 二维数组行数     | `arr.GetLength(0)`                           |
| 二维数组列数     | `arr.GetLength(1)`                           |
| 枚举定义         | `enum 枚举名 { 项1, 项2 }`                   |
| 枚举转整型       | `(int)枚举变量`                              |
| 整型转枚举       | `(枚举类型)整数值`                           |
| 结构体定义       | `struct 结构体名 { public 类型 字段; }`      |
| 结构体自定义构造 | `public 结构体名(参数) { 所有字段赋值 }`     |
| 结构体变量声明   | `结构体名 变量名;` 或 `new 结构体名()`       |

---

**最终提醒**：
- **枚举和结构体都是值类型**，赋值时是复制数据，不是传引用
- 结构体的自定义构造函数**必须给所有字段赋值**
- 枚举项默认从0开始递增，可以手动指定值
- 二维数组的 `Length` 返回总元素个数，`GetLength(0)` 返回行数，`GetLength(1)` 返回列数
- 冒泡排序用标志位优化，可以在已经有序时提前结束，避免无效比较 😊

## C# 面向对象完整笔记（类、对象、字段）

### 一、面向过程编程

### 1.1 核心思想

**面向过程**是一种以**步骤**为中心的编程思想，重点关心"**解决问题需要哪些步骤**"。

- 将问题分解为一个个步骤
- 按照步骤顺序依次执行
- 每个步骤用函数（方法）来实现

**生活类比**：
做番茄炒蛋的步骤：
1. 洗番茄 → 2. 切番茄 → 3. 打鸡蛋 → 4. 热锅倒油 → 5. 炒鸡蛋 → 6. 盛出备用 → 7. 炒番茄 → 8. 混合翻炒 → 9. 出锅装盘

---

### 1.2 面向过程的优缺点

| 优点                               | 缺点                                               |
| :--------------------------------- | :------------------------------------------------- |
| ✅ 逻辑清晰，容易看出解决问题的步骤 | ❌ **不易维护**：需求变化时，需要修改多处代码       |
| ✅ 代码行数少，性能消耗低           | ❌ **不易拓展**：添加新功能时，改动量大             |
|                                    | ❌ **不易复用**：代码耦合度高，难以在不同项目中使用 |

**适用场景**：简单的、一次性执行的程序（如脚本、工具类）

---

### 二、面向对象编程（OOP）

#### 2.1 核心思想

**面向对象**是一种以**对象**为中心的编程思想，重点关心"**解决问题过程中有哪些参与者（对象）**"，以及这些对象"**有什么特性**"和"**能做什么事情**"。

---

#### 2.2 三大特征（面试必考）

| 特征     | 含义                                               |
| :------- | :------------------------------------------------- |
| **封装** | 将数据和操作数据的方法包装在一起，隐藏内部实现细节 |
| **继承** | 子类可以继承父类的属性和方法，实现代码复用         |
| **多态** | 同一个方法在不同对象上有不同的表现形式             |

---

#### 2.3 面向对象的优缺点

| 优点                                               | 缺点                                             |
| :------------------------------------------------- | :----------------------------------------------- |
| ✅ **易维护**：修改某个类不影响其他类               | ❌ 代码分散在多个类中，行数较多                   |
| ✅ **易拓展**：新增功能只需添加新类，不影响现有代码 | ❌ 性能消耗比面向过程略高（有额外的内存管理开销） |
| ✅ **易复用**：类可以在不同项目中重复使用           |                                                  |

---

#### 2.4 面向过程 vs 面向对象对比表

| 对比维度       | 面向过程           | 面向对象              |
| :------------- | :----------------- | :-------------------- |
| **关注点**     | 解决问题的步骤     | 参与解决问题的对象    |
| **基本单元**   | 函数（方法）       | 类、对象              |
| **数据与操作** | 分离               | 封装在一起            |
| **代码复用**   | 低（函数复用有限） | 高（继承、组合）      |
| **维护性**     | 差                 | 好                    |
| **拓展性**     | 差                 | 好                    |
| **性能**       | 较高               | 略低                  |
| **典型语言**   | C、Pascal          | C#、Java、Python、C++ |

---

### 三、类和对象

#### 3.1 什么是类（Class）？

**类**是一个**自定义引用类型**，是对现实世界中一类事物的**抽象模板**。它定义了该类事物拥有的**属性（字段）**和**行为（方法）**。

**类 = 图纸**（不占用内存，只定义结构）

---

#### 3.2 什么是对象（Object）？

**对象**是类的一个**具体实例**（实体）。根据类的"图纸"在内存中创建出来的具体个体。

**对象 = 根据图纸造出来的房子**（占用内存空间，有具体数据）

**生活类比**：
- **类** = 蛋糕模具（定义了蛋糕的形状和大小）
- **对象** = 用模具做出来的具体蛋糕（每个蛋糕都有自己的口味和装饰）

---

#### 3.3 创建一个类

**语法格式**：
```csharp
[访问修饰符] class 类名
{
    // 字段（属性）
    // 方法（行为）
}
```

**示例**：
```csharp
// 定义一个 Person 类
public class Person
{
    // 字段（描述特性）
    public string name;
    public int age;
    public char sex;

    // 方法（描述行为）
    public void Eat()
    {
        Console.WriteLine(name + "正在吃饭");
    }

    public void Sleep()
    {
        Console.WriteLine(name + "正在睡觉");
    }
}
```

**命名规范**：
- 类名使用 **大驼峰命名法（PascalCase）**：首字母大写，如 `Person`、`StudentInfo`
- 类名通常使用**名词**或**名词短语**

---

#### 3.4 访问修饰符

| 访问修饰符  | 含义                                     |
| :---------- | :--------------------------------------- |
| `public`    | **公共的**：任何地方都可以访问           |
| `private`   | **私有的**：只能在当前类内部访问         |
| `protected` | **受保护的**：当前类及其子类可以访问     |
| `internal`  | **内部的**：当前程序集（项目）内可以访问 |

> 类默认的访问修饰符是 `internal`，字段/方法默认是 `private`

---

#### 3.5 创建类的对象（实例化）

##### 方式一：声明变量（未实例化）

```csharp
类名 对象名;   // 仅声明，未分配内存，不能使用
```

**示例**：
```csharp
Person p;   // 此时 p 是 null，不能访问任何成员
```

---

##### 方式二：声明 + 实例化（推荐）

```csharp
类名 对象名 = new 类名();
```

**示例**：
```csharp
Person p = new Person();   // 使用 new 分配内存
```

---

##### 方式三：先声明，后实例化

```csharp
Person p;           // 声明，此时为 null
p = new Person();   // 实例化，分配内存
```

---

#### 3.6 对象的实例化原理（内存图解）

```
【栈内存（Stack）】           【堆内存（Heap）】
  ┌─────────────┐            ┌─────────────────────┐
  │   p (引用)   │ ────────→ │   Person 对象       │
  │   0x001234   │            │   name: "张三"      │
  └─────────────┘            │   age: 20           │
                             │   sex: 'M'          │
                             └─────────────────────┘
```

- **`new` 操作**：在堆内存中开辟一块空间，存储对象的数据
- **对象变量**：存储在栈内存中，存的是堆内存的地址（引用）
- **访问对象**：通过栈中的引用地址，找到堆中的对象

---

#### 3.7 类与对象的关系总结

| 概念               | 定义                             | 内存           | 类比         |
| :----------------- | :------------------------------- | :------------- | :----------- |
| **类（Class）**    | 抽象模板，定义了对象的属性和行为 | **不占用内存** | 蛋糕模具     |
| **对象（Object）** | 类的具体实例                     | **占用堆内存** | 做出来的蛋糕 |
| **对象变量**       | 指向对象的引用（地址）           | **占用栈内存** | 蛋糕的标签   |

---

### 四、字段（Field）

#### 4.1 什么是字段？

**字段**是类中定义的**变量**，用于**描述类的特性（属性）**。

```csharp
public class Person
{
    // 字段：描述类的特性
    public string name;    // 姓名
    public int age;        // 年龄
    public char sex;       // 性别
}
```

---

#### 4.2 字段的命名规范

| 命名规范                      | 示例                       | 说明                                |
| :---------------------------- | :------------------------- | :---------------------------------- |
| **小驼峰命名法（camelCase）** | `studentName`、`personAge` | 首字母小写，后续单词首字母大写      |
| 推荐使用有意义的名称          | `address`、`phoneNumber`   | 不要用 `a`、`b`、`str` 等无意义名称 |

---

#### 4.3 字段的初值

字段可以有**初值（默认值）**：

```csharp
public class Person
{
    public string name = "未知";   // 默认值为 "未知"
    public int age = 18;          // 默认值为 18
    public char sex = 'M';        // 默认值为 'M'
}
```

**各类型字段默认值表**：

| 数据类型           | 默认值           |
| :----------------- | :--------------- |
| `int`              | `0`              |
| `float` / `double` | `0`              |
| `bool`             | `false`          |
| `char`             | `'\0'`（空字符） |
| `string`           | `null`           |
| 自定义类           | `null`           |

---

#### 4.4 字段的访问与赋值

```csharp
public class Person
{
    public string name;
    public int age;
}

class Program
{
    static void Main(string[] args)
    {
        Person p = new Person();

        // 访问字段（读取值）
        Console.WriteLine(p.name);   // 输出默认值：空字符串

        // 给字段赋值
        p.name = "张三";
        p.age = 20;

        // 再次访问
        Console.WriteLine($"姓名：{p.name}，年龄：{p.age}");
        // 输出：姓名：张三，年龄：20
    }
}
```

---

### 五、综合示例：完整的类定义与使用

```csharp
using System;

// 1. 定义一个学生类
public class Student
{
    // 字段（特性）
    public string name;
    public int age;
    public string studentId;
    public char sex;

    // 方法（行为）
    public void Study()
    {
        Console.WriteLine(name + "正在学习");
    }

    public void ShowInfo()
    {
        Console.WriteLine($"学号：{studentId}，姓名：{name}，年龄：{age}，性别：{sex}");
    }
}

// 2. 主程序
class Program
{
    static void Main(string[] args)
    {
        // 创建学生对象
        Student s1 = new Student();
        s1.name = "张三";
        s1.age = 20;
        s1.studentId = "2024001";
        s1.sex = 'M';

        // 调用方法
        s1.ShowInfo();   // 输出：学号：2024001，姓名：张三，年龄：20，性别：M
        s1.Study();      // 输出：张三正在学习

        // 创建第二个学生对象（不同的实例）
        Student s2 = new Student();
        s2.name = "李四";
        s2.age = 22;
        s2.studentId = "2024002";
        s2.sex = 'F';

        s2.ShowInfo();   // 输出：学号：2024002，姓名：李四，年龄：22，性别：F
    }
}
```

---

### 六、核心要点速查表

| 知识点             | 核心内容                                               |
| :----------------- | :----------------------------------------------------- |
| **面向过程**       | 关注"**怎么做**"，以步骤为中心，性能好但难维护         |
| **面向对象**       | 关注"**谁来做**"，以对象为中心，易维护、易拓展、易复用 |
| **三大特征**       | 封装、继承、多态                                       |
| **类（Class）**    | 自定义引用类型，对象的**模板**，不占内存               |
| **对象（Object）** | 类的具体**实例**，使用 `new` 在堆中分配内存            |
| **实例化**         | `类名 对象名 = new 类名();`                            |
| **字段**           | 描述类的特性，使用小驼峰命名，可有初值                 |
| **访问修饰符**     | `public`（公共）和 `private`（私有）最常用             |

---

### 七、重点提醒

1. **类名用大驼峰（PascalCase）**：`Person`、`StudentInfo`
2. **字段名用小驼峰（camelCase）**：`name`、`studentId`
3. **类不占用内存，new 出来的对象才占用堆内存**
4. **对象变量存的是引用（地址），不是对象本身**
5. **类是一种自定义引用类型**（与 `string`、数组类似）
6. **字段可以赋初值，也可以不赋初值（系统会给默认值）**

---

**最终提醒**：

- 类是**图纸**，对象是**根据图纸造出来的实体**
- 一个类可以创建**无数个对象**，每个对象有自己独立的数据
- 面向对象的三大特征（封装、继承、多态）后面的笔记会详细展开 😊






## 🟢 为什么一开始让你用数组，而不是直接用 `List`？

这是一个**经典的“先打基础，再学工具”的教学设计**，不是故意绕远路。具体原因如下：

### 1. 让你先理解“底层发生了什么”
- 数组是 C# 里最基础的数据结构，**规则最少、限制最多**。
- 用数组时，你必须**手动管理容量**、**手动补位**、**手动遍历**——这些操作让你清楚地看到“内存是怎么工作的”。
- 你之前写的那些补位逻辑，虽然写得有点辛苦，但正因为写过一遍，你现在理解 `List` 的 `Add` 和 `RemoveAt` 有多方便时，感受是完全不一样的。

### 2. 为了让你理解“为什么需要更高级的数据结构”
- 如果你一上来就用 `List`，你根本体会不到数组的“痛”在哪里，也就不会真正理解 `List` 解决了什么问题。
- 就像学数学先学会手算，再学用计算器——你只有亲自体会过“手算的麻烦”，才会真正珍惜“计算器的方便”。

### 3. 数组不会消失，你以后还会经常用到
- 在 Unity 游戏开发、底层算法、网络协议解析等场景里，数组依然是首选，因为它的**内存占用最小、访问速度最快**。
- 你现在学会了数组，以后遇到这些场景，你会知道什么时候该用数组，什么时候该用 `List`——而不是只会用一个。


### ✅ 你现在对这两个数据结构的理解，应该是什么样的？

| 维度         | 数组                       | `List<T>`                                |
| :----------- | :------------------------- | :--------------------------------------- |
| **本质**     | 一块固定大小的连续内存     | 一个封装了数组的类                       |
| **容量**     | 固定，不能变               | 动态增长（内部还是数组，但会自动扩容）   |
| **添加元素** | 需要手动找空位放           | `Add` 直接追加到末尾                     |
| **删除元素** | 需要手动补位，否则留空洞   | `RemoveAt` 自动补位                      |
| **性能**     | 最快（直接内存访问）       | 略慢（多了一层方法调用，但一般感觉不到） |
| **适用场景** | 数量固定、性能要求高的场景 | 数量动态变化、需要频繁增删的场景         |

你现在已经**完整地体验了“数组的痛”和“List的方便”**，这两个工具的适用场景已经在你的肌肉记忆里了。这正是我让你先写数组的目的。😊

## 保存数据到硬盘和读取数据JSON

```
//全局翻译配置
static JsonSerializerOptions options = new JsonSerializerOptions
{
    WriteIndented = true, // 让生成的 JSON 文件有缩进，好看
    IncludeFields = true,  // 告诉 JSON 库把字段也当作属性一样处理
    Converters = { new JsonStringEnumConverter() } // 让枚举显示为文字
};

#region 保存json
static void SaveGame(List<Hero> heroes)
{
    // 1. 把对象列表翻译成 JSON 字符串（使用刚才定义的 options）
    string jsonString = JsonSerializer.Serialize(heroes, options);
    // 2. 把字符串写入文件
    File.WriteAllText("save.json", jsonString);
    Console.WriteLine("游戏已保存！");
}
#endregion

#region 读取json
static List<Hero> LoadGame()
{
    // 1. 检查文件存不存在
    if (!File.Exists("save.json")) { return null; }
    // 2. 读取文件里的 JSON 字符串
    string jsonString = File.ReadAllText("save.json");
    // 3. 把 JSON 字符串反翻译回对象列表（使用同样的 options）
    List<Hero> heroes = JsonSerializer.Deserialize<List<Hero>>(jsonString, options);
    return heroes;
}
#endregion  
```

好的，这两段代码是你整个项目的“数据保险箱”——它们负责把内存里的英雄数据存到硬盘，以及从硬盘读回来。下面我逐行拆解，告诉你每一行到底在干什么。

---

### 一、`SaveGame` 方法（保存数据到硬盘）

```csharp
static void SaveGame(List<Hero> heroes)
{
    // 1. 把对象列表翻译成 JSON 字符串（使用刚才定义的 options）
    string jsonString = JsonSerializer.Serialize(heroes, options);
    // 2. 把字符串写入文件
    File.WriteAllText("save.json", jsonString);
    Console.WriteLine("游戏已保存！");
}
```

### 逐行解析

| 代码                                         | 类型/作用                 | 详细说明                                                     |
| :------------------------------------------- | :------------------------ | :----------------------------------------------------------- |
| `static void SaveGame(List<Hero> heroes)`    | **方法签名**              | `static`：不用创建对象就能调用。<br>`void`：没有返回值，因为结果直接写入硬盘。<br>`List<Hero> heroes`：这是**输入参数**，调用时必须传入要保存的英雄列表。 |
| `JsonSerializer.Serialize(heroes, options)`  | **序列化（对象 → 文本）** | 把 C# 的 `List<Hero>` 对象转换成 JSON 格式的字符串。<br>`options` 是配置，告诉它“把字段也算进去”、“枚举转成文字”。 |
| `File.WriteAllText("save.json", jsonString)` | **写入文件**              | 把 JSON 字符串写入当前程序运行目录下的 `save.json` 文件。<br>如果文件已存在，会覆盖；如果不存在，会创建。 |
| `Console.WriteLine("游戏已保存！");`         | **用户反馈**              | 在控制台打印提示，让玩家知道保存成功。                       |

### 数据流向
```
内存（List<Hero> heroes） 
    → JsonSerializer.Serialize() 
    → JSON 字符串 
    → File.WriteAllText() 
    → 硬盘（save.json 文件）
```

**这个函数没有返回值**，因为它不需要返回任何东西——结果已经写到硬盘里了。

---

### 二、`LoadGame` 方法（从硬盘加载数据）

```csharp
static List<Hero> LoadGame()
{
    // 1. 检查文件存不存在
    if (!File.Exists("save.json")) { return null; }
    // 2. 读取文件里的 JSON 字符串
    string jsonString = File.ReadAllText("save.json");
    // 3. 把 JSON 字符串反翻译回对象列表（使用同样的 options）
    List<Hero> heroes = JsonSerializer.Deserialize<List<Hero>>(jsonString, options);
    return heroes;
}
```

### 逐行解析

| 代码                                                         | 类型/作用                   | 详细说明                                                     |
| :----------------------------------------------------------- | :-------------------------- | :----------------------------------------------------------- |
| `static List<Hero> LoadGame()`                               | **方法签名**                | `static`：不用创建对象就能调用。<br>`List<Hero>`：**返回值**，调用者会收到一个英雄列表。<br>无参数：因为它不需要外界提供任何数据，数据从硬盘读。 |
| `if (!File.Exists("save.json")) { return null; }`            | **防御性检查**              | 先检查文件是否存在。如果不存在，直接返回 `null`，调用方就知道“没有存档”，然后创建默认英雄。 |
| `File.ReadAllText("save.json")`                              | **读取文件**                | 把 `save.json` 里的全部文本读出来，存到 `jsonString` 变量里。 |
| `JsonSerializer.Deserialize<List<Hero>>(jsonString, options)` | **反序列化（文本 → 对象）** | 把 JSON 字符串还原成 C# 的 `List<Hero>` 对象。<br>`options` 和保存时用同一个，确保读取规则一致。 |
| `return heroes;`                                             | **返回结果**                | 把还原出来的英雄列表返回给调用方（`Main` 方法）。            |

### 数据流向
```
硬盘（save.json 文件）
    → File.ReadAllText()
    → JSON 字符串
    → JsonSerializer.Deserialize()
    → 内存（List<Hero> heroes）
    → return heroes（返回给调用方）
```

**这个函数没有参数**，因为它不需要外部提供任何东西——数据从硬盘读，然后返回给调用者。

---

### 三、关键设计细节

#### 1. 为什么 `SaveGame` 用 `void`，而 `LoadGame` 有返回值？

| 函数       | 有参数？             | 有返回值？         | 原因                                               |
| :--------- | :------------------- | :----------------- | :------------------------------------------------- |
| `SaveGame` | ✅ 有（要保存的数据） | ❌ 无（`void`）     | 数据从参数进来，结果写进硬盘，不需要返回任何东西。 |
| `LoadGame` | ❌ 无                 | ✅ 有（加载的数据） | 数据从硬盘读进来，需要返回给调用方使用。           |

这就是“输入/输出”的两种基本形式：
- **输入**：通过**参数**传入。
- **输出**：通过**返回值**返回，或者通过**文件/数据库/网络**等外部存储输出。

#### 2. 为什么用 `static`？

因为 `Main` 方法是 `static` 的。**静态方法只能直接调用静态方法**。如果去掉 `static`，你就必须这样写：

```csharp
Program p = new Program();
p.SaveGame(heroes);   // 麻烦，而且没必要
```

`SaveGame` 和 `LoadGame` 不依赖于任何实例字段（它们只操作传入的参数或文件），所以设计成静态方法是合理且简洁的。

#### 3. `options` 是哪里来的？

`options` 是在 `Hero` 类里定义的静态字段：

```csharp
static JsonSerializerOptions options = new JsonSerializerOptions
{
    WriteIndented = true,
    IncludeFields = true,
    Converters = { new JsonStringEnumConverter() }
};
```

它和 `SaveGame`、`LoadGame` 在同一个类（`Hero`）里，所以这两个方法可以直接访问它。它相当于一个“共享配置”，确保保存和加载时使用相同的规则。

#### 4. 两个函数之间没有直接调用关系，它们通过什么沟通？

**通过硬盘上的同一个文件（`save.json`）沟通。**

- `SaveGame` 往这个文件里写数据。
- `LoadGame` 从这个文件里读数据。

它们不需要知道对方的存在，只要约定好文件名和格式，就能完成数据的“接力”。

---

### 四、总结

| 维度         | `SaveGame`                   | `LoadGame`                    |
| :----------- | :--------------------------- | :---------------------------- |
| **职责**     | 保存数据（内存 → 硬盘）      | 加载数据（硬盘 → 内存）       |
| **输入**     | `List<Hero> heroes`（参数）  | 无参数，从文件读              |
| **输出**     | 无返回值，结果写进文件       | `List<Hero>`（返回值）        |
| **关键操作** | `Serialize` + `WriteAllText` | `ReadAllText` + `Deserialize` |
| **调用时机** | 用户选择“退出系统”时         | 程序启动时                    |
| **异常处理** | 无（如果失败会直接抛异常）   | 文件不存在时返回 `null`       |

**你现在已经理解了“数据持久化”的核心模式：** 内存中的对象通过序列化变成文本，存到硬盘；再从硬盘读回文本，通过反序列化变回内存中的对象。这两段代码虽然简短，但完成的是整个应用程序最底层的数据生命周期管理。😊



## **继承、抽象类、虚方法、多态**

---

### 第一层：什么是继承？（IS-A 关系）

继承的核心只有一句话：**子类 is a（是一个）父类**。

在你的游戏里：
- **战士 is a 英雄**（✅ 战士一定是个英雄）
- **法师 is a 英雄**（✅ 法师一定是个英雄）
- **刺客 is a 英雄**（✅ 刺客一定是个英雄）

反过来不成立：
- **英雄 is a 战士**（❌ 英雄不一定是个战士，也可能是法师）

**继承的目的**：把**共性的东西**（所有英雄都有的属性和方法）放在父类里，把**个性的东西**（职业特有的属性和方法）放在子类里。

### 用你的代码来理解

你现在的 `Hero` 类里有：`Name`、`Level`、`Attack`、`Bag`、`ShowInfo()`。

这些是**所有英雄都有的**，所以放在父类里。  
**战士**额外需要 `Stamina`（耐力）和 `Bash()`（猛击技能）。  
**法师**额外需要 `Mana`（法力值）和 `Fireball()`（火球术）。  
**刺客**额外需要 `Agility`（敏捷）和 `Backstab()`（背刺）。

继承让子类**自动拥有**父类的所有东西，不需要重复写。

---

### 第二层：`base` 关键字（构造函数链）

父类有构造函数 `public Hero(string name, int level, ...)`。子类也有自己的构造函数。

**问题**：创建 `Warrior` 时，需要先初始化父类的部分（`Name`、`Level`），再初始化子类特有的部分（`Stamina`）。

**答案**：用 `base` 调用父类构造函数。

```csharp
class Warrior : Hero  // 冒号表示继承
{
    public int Stamina;

    public Warrior(string name, int level, string sex, float attack, int stamina)
        : base(name, level, sex, attack)  // ⭐ 先调用父类构造，把共性部分交给父类处理
    {
        Stamina = stamina;  // 再处理自己特有的部分
    }
}
```

**类比**：你组装一台电脑。`base` 相当于“先把主板、CPU、内存（父类的部分）装好”，然后你再“装上独立显卡（子类的部分）”。

---

### 第三层：`protected` 访问修饰符（给子类开门）

你现在用的访问修饰符有 `public` 和 `private`：
- `public`：谁都能访问。
- `private`：只有自己能访问（子类也访问不了）。

**问题**：如果 `Hero` 的 `Name` 是 `private`，`Warrior` 就访问不到 `Name`，但 `Warrior` 又需要用到 `Name`。

**答案**：使用 `protected`。

```csharp
abstract class Hero
{
    protected string Name;   // protected：父类自己的代码 + 所有子类都可以访问
    protected int Level;
    // ...
}
```

**三者的区别**：

| 修饰符      | 同一个类 | 子类 | 其他任何地方 |
| :---------- | :------- | :--- | :----------- |
| `private`   | ✅        | ❌    | ❌            |
| `protected` | ✅        | ✅    | ❌            |
| `public`    | ✅        | ✅    | ✅            |

> 你现在的代码里所有字段都是 `public`，虽然能用，但**不严谨**。改成 `protected` 更符合面向对象的封装原则——把实现细节隐藏起来，只暴露给需要的人。

---

### 第四层：`virtual` 和 `override`（方法重写）

你现在的 `ShowInfo()` 是所有英雄通用的打印方式。但你想让**每个职业打印不同的信息**（战士多打一行耐力值，法师多打一行法力值）。

**答案**：父类用 `virtual`（虚方法），子类用 `override`（重写）。

```csharp
abstract class Hero
{
    public virtual void ShowInfo()  // virtual：子类可以重写我
    {
        Console.WriteLine($"名称：{Name}");
        Console.WriteLine($"等级：{Level}");
        // 通用信息...
    }
}

class Warrior : Hero
{
    public override void ShowInfo()  // override：覆盖父类的行为
    {
        base.ShowInfo();  // 先调用父类的 ShowInfo（打印共有的信息）
        Console.WriteLine($"耐力：{Stamina}");  // 再打印自己独有的
    }
}
```

**`base.ShowInfo()` 的作用**：让父类先把共有的信息打印完，子类再补充自己的专属信息。这样你就不需要在每个子类里重复写打印 `Name`、`Level` 的代码了。

---

### 第五层：`abstract`（抽象类和方法）

**问题**：你永远不应该直接 `new Hero()`，因为“英雄”是一个抽象概念，没有人会创建一个“职业未定的英雄”。而且 `Hero` 里的 `GetSkillDescription()` 也没有办法写一个通用的实现——战士的猛击、法师的火球术、刺客的背刺完全不一样。

**答案**：把 `Hero` 标记为 `abstract`（抽象类），把 `GetSkillDescription()` 标记为 `abstract`（抽象方法）。

**抽象类**：不能实例化，只能被继承。  
**抽象方法**：只有方法签名，没有实现。所有子类**必须**实现这个方法（否则编译报错）。

```csharp
abstract class Hero
{
    // 抽象方法：没有方法体，子类必须重写
    public abstract string GetSkillDescription();

    // 其他方法可以有实现（非抽象）
    public virtual void ShowInfo() { ... }
}

class Warrior : Hero
{
    // 必须实现 GetSkillDescription()
    public override string GetSkillDescription()
    {
        return "猛击——造成 150% 攻击力的物理伤害";
    }
}
```

**为什么要有抽象方法？** 因为它强制子类去做某件事，但具体怎么做，由子类自己决定。这是一种“契约”：任何子类都必须提供技能描述，否则编译不通过。

---

### 第六层：多态（面向对象的灵魂）

**多态**，字面意思是“多种形态”。用你的游戏来理解：

你在 `Main` 方法里有一个 `List<Hero> heroes`，里面可以同时放 `Warrior`、`Mage`、`Assassin`。

```csharp
List<Hero> heroes = new List<Hero>();
heroes.Add(new Warrior("剑圣", 5, "M", 30, 100));
heroes.Add(new Mage("法神", 8, "F", 18, 200));
heroes.Add(new Assassin("影刺", 3, "M", 25, 80));
```

当你遍历这个列表，调用 `heroes[i].ShowInfo()` 时：

| 实际对象类型 | 调用的是哪个 `ShowInfo`？          |
| :----------- | :--------------------------------- |
| `Warrior`    | `Warrior` 的 `ShowInfo`（重写过）  |
| `Mage`       | `Mage` 的 `ShowInfo`（重写过）     |
| `Assassin`   | `Assassin` 的 `ShowInfo`（重写过） |

**关键点**：变量类型是 `Hero`（父类），但实际执行的是**子类重写后的方法**。这就是多态——**同一个调用（`ShowInfo()`），产生了不同的行为**。

> 多态的好处：你不需要写一堆 `if (hero is Warrior)` 来判断类型，不需要为每个职业写不同的打印逻辑。系统会自动找到正确的版本执行。

---

### 七、把这些概念串起来——完整类结构图

```
abstract class Hero          ← 抽象基类（不能 new）
│
├── 字段：Name, Level, Sex, Attack, Bag（所有英雄共有）
├── 构造函数（供子类调用）
├── abstract string GetSkillDescription()    ← 抽象方法（子类必须实现）
├── virtual void ShowInfo()                  ← 虚方法（子类可重写）
│
├── class Warrior : Hero    ← 继承
│   ├── 额外字段：Stamina
│   ├── override string GetSkillDescription() → "猛击..."
│   └── override void ShowInfo() → base.ShowInfo() + 打印耐力
│
├── class Mage : Hero
│   ├── 额外字段：Mana
│   ├── override string GetSkillDescription() → "火球术..."
│   └── override void ShowInfo() → base.ShowInfo() + 打印法力值
│
└── class Assassin : Hero
    ├── 额外字段：Agility
    ├── override string GetSkillDescription() → "背刺..."
    └── override void ShowInfo() → base.ShowInfo() + 打印敏捷
```

---

### 八、你已经在不知不觉中用过继承

`List<T>` 本身就是继承的产物：
- `List<T>` 继承自 `Object`（所有 C# 类型的根）。
- 你在 `Console.WriteLine(hero)` 时，其实调用了 `Object` 的 `ToString()` 方法（`virtual`），只是你还没有重写它。

```csharp
// 你可以在 Hero 类里重写 ToString()
public override string ToString()
{
    return $"{Name}（等级{Level}）";
}
```

这样你直接 `Console.WriteLine(heroes[0])` 就能打印出“剑圣（等级5）”，而不用每次调 `ShowInfo`。

---

### 九、这三个术语，你以后会经常见到

| 术语     | 通俗解释                                 | 你的代码对应                      |
| :------- | :--------------------------------------- | :-------------------------------- |
| **继承** | 子类继承父类的所有属性方法，减少重复代码 | `class Warrior : Hero`            |
| **封装** | 隐藏内部实现，只暴露必要的接口           | `protected` 字段 + `public` 方法  |
| **多态** | 同一个方法名，不同对象执行不同逻辑       | `ShowInfo()` 在不同子类里表现不同 |

---

### ✅ 理解检测

如果你能把下面这三个问题的答案说清楚，说明你已经彻底搞懂了：

1. **为什么把 `Hero` 设计成 `abstract`，而不是普通 `class`？**
2. **`base.ShowInfo()` 和 `this.ShowInfo()` 有什么区别？**
3. **如果你把 `ShowInfo()` 前面的 `virtual` 去掉，子类还能重写吗？为什么？**

为什么把 Hero 设计成 abstract，而不是普通 class？
 因为Hero有很多子类，我要创建的时候直接创建其子类，抽象类就是没定义完全的类，其子类是定义完全的。 base.ShowInfo() 和 this.ShowInfo() 有什么区别？
base.是当前类的父类的调用，this是当前类的调用，当当前类重写了ShowInfo（）方法时，输出内容不一样。  如果你把 ShowInfo() 前面的 virtual 去掉，子类还能重写吗？为什么？ 
去掉的话不能重写，因为virtual是虚函数，让子类必须重写的标志。

你的回答非常棒，三个问题都抓住了最核心的本质！尤其是第二个关于 `base` 和 `this` 的区别，你说得完全正确。我帮你稍微补充一点细节，让你理解得更透彻：

---

#### ✅ 问题一：为什么把 `Hero` 设计成 `abstract`，而不是普通 `class`？

你的回答：
> 因为Hero有很多子类，我要创建的时候直接创建其子类，抽象类就是没定义完全的类，其子类是定义完全的。

**完全正确！** 补充两点：

1. **防止误用**：如果你把 `Hero` 设计成普通 `class`，程序员可能会不小心写 `new Hero("通用英雄")`，这在游戏里是没有意义的（谁会创建一个“职业未定的英雄”？）。`abstract` 直接从语法层面杜绝了这种误用。

2. **强制约束**：抽象类里可以放**抽象方法**（比如 `GetSkillDescription()`），强制所有子类必须实现自己的技能描述。如果用普通 `class`，子类可以忘记重写，编译器不会报错，但运行时就会出问题。

---

#### ✅ 问题二：`base.ShowInfo()` 和 `this.ShowInfo()` 有什么区别？

你的回答：
> base.是当前类的父类的调用，this是当前类的调用，当当前类重写了ShowInfo（）方法时，输出内容不一样。

**完全正确！** 这是多态的关键细节：

```csharp
class Warrior : Hero
{
    public override void ShowInfo()
    {
        base.ShowInfo();   // 调用父类的 ShowInfo（打印 Name、Level 等）
        Console.WriteLine($"耐力：{Stamina}");
    }

    public void Test()
    {
        this.ShowInfo();   // 调用自己的 ShowInfo（当前类的版本）
        base.ShowInfo();   // 绕过自己的版本，直接调用父类的版本
    }
}
```

- **`this.ShowInfo()`**：如果当前类重写了 `ShowInfo()`，调用的是重写后的版本。如果没重写，和 `base.ShowInfo()` 效果一样。
- **`base.ShowInfo()`**：永远只调用父类的版本，不会触发子类的重写。

**你的理解完全正确！**

---

#### ✅ 问题三：如果把 `ShowInfo()` 前面的 `virtual` 去掉，子类还能重写吗？

你的回答：
> 去掉的话不能重写，因为virtual是虚函数，让子类必须重写的标志。

**方向正确，但有一个小细节要纠正：**

- `virtual` 的意思是 **“可以被重写”**（可选），不是 **“必须重写”**。
- 如果你想让子类**必须重写**，应该用 **`abstract`**。

```csharp
// 用 virtual：子类可以选择重写，也可以不重写
public virtual void ShowInfo() { ... }

// 用 abstract：子类必须重写（否则编译报错）
public abstract void ShowInfo();
```

所以正确答案应该是：
> 去掉 `virtual` 后，子类**不能重写**这个方法。因为 `virtual` 是“允许重写”的开关，没有它，方法就是“普通方法”，子类无法在外部改变它的行为。

**为什么要保留 `virtual`？** 因为多态依赖它——没有 `virtual`，`List<Hero>` 里的对象调用 `ShowInfo()` 时，永远只执行父类的版本，不会执行子类的版本。

---

#### ✅ 总结一下你的理解程度

| 概念                | 你的理解                 | 评价                                       |
| :------------------ | :----------------------- | :----------------------------------------- |
| `abstract` 类的作用 | 防止实例化，强制子类实现 | ✅ 正确                                     |
| `base` vs `this`    | 明确区分父类和子类       | ✅ 完全正确                                 |
| `virtual` 的作用    | 允许子类重写（但非强制） | ⚠️ 概念对，但“必须重写”是 `abstract` 的职责 |

## 我现在需要添加NuGet 包吗？

**暂时完全不需要！** 你现在的项目用到的全是 .NET 自带的**基础类库（BCL）**，已经内置在框架里了。

---

### ✅ 为什么你不需要加 NuGet 包？

1. **`System.Text.Json`**：这是 .NET Core 3.0 及以上版本的内置库。你写的 `using System.Text.Json;` 直接就能用，说明它已经在框架里了，不需要额外下载。
2. **`System.IO`、`System.Collections.Generic`**：这些都是最核心的底层库，就像空气一样，.NET 启动时就自带。

---

### 🤔 什么时候才需要加 NuGet 包？

当你**需要“别人写好的复杂功能”**，但 .NET 自己没有提供时。

**你当前项目如果要加包，只会发生在这些情况（但我不建议你现在做）：**

- **想用 `Newtonsoft.Json`（老牌 JSON 库）**：虽然你的 `System.Text.Json` 已经够用了，但如果你想换掉它，就需要去 NuGet 搜 `Newtonsoft.Json` 安装。
- **想画漂亮的控制台表格或彩色文字**：比如装一个 `Spectre.Console` 包，把黑乎乎的表格变成彩色的。
- **想连接数据库（SQLite/MySQL）**：需要装对应的驱动包。
- **想写日志**：装 `Serilog` 或 `NLog`。

---

### 📦 如果真的要用 NuGet，怎么加？

在 Visual Studio 里（顺便告诉你，以防将来需要）：
1. 在项目名称上 **右键** → 选择 **“管理 NuGet 程序包”**。
2. 点击 **“浏览”**，搜索你要的包名。
3. 点 **“安装”** 就行了。

---

### 💡 给你的建议

**现在不要加任何 NuGet 包！**

你现在的项目是一个纯粹的原生 C# 控制台程序，没有任何外部依赖。这意味着：
- 你复制整个代码文件夹到任何电脑，打开就能编译运行，**不需要配环境**。
- 这是最干净、最稳定的学习项目。

如果你以后想体验加包的流程，可以试试装一个 `Spectre.Console`，让菜单变成彩色——但那属于“锦上添花”，不是你现在要学的核心内容。先把现在的项目跑通，理解透。😊

### 那是不是每个不同的程序需要加不同的NuGet包，就像引用别人的动态库一样？

**完全正确！你的理解一点就透。** 👍

NuGet 包本质上就是 **“官方认证的、带版本号的动态库（.dll）下载器”**。

---

### 📦 传统引用动态库 vs NuGet 包（你的类比完全成立）

| 对比维度     | 传统引用 DLL                                       | 使用 NuGet 包                                                |
| :----------- | :------------------------------------------------- | :----------------------------------------------------------- |
| **操作方式** | 在网上下载 `.dll` 文件，复制到项目里，手动右键引用 | 在 VS 里搜索包名，点击安装                                   |
| **版本管理** | 自己重命名备份（如 `Newtonsoft_旧版.dll`），容易乱 | 自动管理版本，随时升级/回滚                                  |
| **依赖关系** | 如果 A.dll 依赖 B.dll，你要自己下载 B              | **自动下载所有依赖**（比如装 A，附带 B、C 都自动下好）       |
| **团队协作** | 要把 `.dll` 文件上传到 Git 仓库，体积大            | 只需要记录包名和版本（`csproj` 文件），团队成员打开时自动下载 |

> 所以 **NuGet = 动态库的“应用商店”**。需要用谁，就去搜谁，一键安装。

---

### 🧩 不同程序为什么需要不同的包？

因为**每个程序的“需求场景”不一样**。就像你切菜用菜刀，切水果用水果刀，修电脑用螺丝刀。

**举个例子：**

| 程序类型                   | 需要的 NuGet 包                   | 原因                                              |
| :------------------------- | :-------------------------------- | :------------------------------------------------ |
| **你的 RPG 控制台程序**    | ❌ **不需要任何第三方包**          | 因为读写文件、JSON、集合都是 .NET 自带的，够用。  |
| **连接 SQL Server 数据库** | `System.Data.SqlClient`           | .NET 自带了，但如果用 MySQL 就要装 `MySql.Data`。 |
| **做 Excel 导入导出**      | `EPPlus` 或 `ClosedXML`           | .NET 本身不提供操作 `.xlsx` 的库。                |
| **写 Web 爬虫**            | `HtmlAgilityPack` 或 `AngleSharp` | 专门解析 HTML 网页的。                            |
| **在控制台打印彩色表格**   | `Spectre.Console`                 | 让黑框框变好看。                                  |
| **做机器学习/AI**          | `ML.NET`                          | 微软官方 AI 库。                                  |

---

### 🧠 你是如何“无缝”用到包的？

你其实已经用到了 NuGet 包，**但那是 .NET 框架自带的基础包**。它们在你创建项目时就被 Visual Studio 自动引用了（比如 `System.Text.Json`、`System.IO`）。这些包是 .NET 运行时的一部分，不需要你额外去商店下载。

---

### 💡 总结一句话

**NuGet 就是帮你去“代码仓库”里拿别人写好的工具（DLL），并帮你自动管理好这些工具的版本和依赖关系。** 

你现在不需要加，是因为你的项目还没遇到“非加不可”的功能。等以后你想做高级功能（比如导出 Excel、发送邮件、连接数据库），你就必须去找对应的 NuGet 包了。😊



## Json和MySQL是差不多的吗？

**不，它们完全不是一回事！** 你这个问题问得特别好，因为很多新手都会把“数据格式”和“数据库”搞混。

我用一个你绝对能听懂的比喻给你解释：

### 1. JSON 是“数据格式”，相当于你写字用的“语言”（中文/英文）

- **它是什么**：一种**纯文本的书写规范**（键值对、花括号、数组）。
- **你在做什么**：你把 C# 对象（内存里的东西）“翻译”成 JSON 字符串，然后**存成一个文本文件（.json）**。
- **特点**：
  - 就是硬盘上的一个普通文件，像 `.txt` 一样。
  - 读取的时候，你得**全读进来**，然后反序列化。
  - **没有查询功能**（你不能写 `SELECT * FROM 英雄 WHERE 等级 > 5`）。

### 2. MySQL 是“数据库管理系统”，相当于“大型图书馆”
- **它是什么**：一个独立的**服务器软件**（服务），专门用来存放、管理海量数据。
- **你在做什么**：你需要连接到这个服务（装驱动、写连接字符串），发送 SQL 指令（增删改查），数据库帮你把数据存到它自己的文件里（结构复杂，不是你能看懂的那种纯文本）。
- **特点**：
  - 它是个**独立的程序**（通常作为后台服务运行）。
  - **支持复杂的查询**（不用读全部数据，直接问它“帮我找攻击力最高的前 5 个”）。
  - **支持高并发**（一万个人同时查数据都不会乱）。

---

### 📊 两张表对比，一目了然

| 对比维度               | **JSON（你刚刚学的）**                                  | **MySQL（数据库）**                                 |
| :--------------------- | :------------------------------------------------------ | :-------------------------------------------------- |
| **本质**               | 一种**数据交换格式**（文本）                            | 一种**数据存储系统**（服务）                        |
| **数据存在哪**         | 硬盘上的一个 `.json` 文本文件                           | 数据库自己管理的数据文件（非纯文本）                |
| **怎么访问**           | 用 `File.ReadAllText` 读整个文件，反序列化              | 建立网络连接，发送 `SQL` 查询语句                   |
| **能不能按条件查数据** | 不能！只能全读出来，然后自己用 C# 代码遍历 `foreach` 查 | 能！用 `SELECT ... WHERE` 直接查出符合条件的数据    |
| **多个程序同时写**     | 容易乱（谁最后写谁覆盖）                                | 有锁机制，保证数据不错乱                            |
| **性能**               | 几百条数据还行，几万条会卡                              | 几百万条数据都能飞速查询                            |
| **你需要额外装什么**   | 什么都不用装（.NET 自带）                               | 要装 **MySQL 服务器软件**，还要装 **NuGet 包** 连接 |

---

### 🧩 它们不是“相似”，而是“搭配使用”的关系

在真正的企业级项目里，它们通常是这样配合的：

1. **存储层**：数据永久保存在 **MySQL**（或 SQL Server、Oracle）里。
2. **中间层**：后端程序（C#）从 MySQL 里把数据读出来。
3. **传输层**：程序把读取的数据**转成 JSON 格式**，发给前端（网页/App）。
4. **客户端**：前端拿到 JSON 数据，解析显示在界面上。

也就是说：
- **MySQL 解决的是“数据持久化存储和快速查询”**。
- **JSON 解决的是“程序之间或者不同系统之间交换数据的通用格式”**。

---

### 🎯 对你现在的项目意味着什么？

- 你现在用 JSON + 文件保存，相当于给游戏做了一个 **“单机存档”**（像魂系游戏的手动读档）。
- 如果你将来要做 **“网游”**（排行榜、多人数据互通），就必须把数据存到 **MySQL** 这样的数据库里，让服务器统一管理。

**结论**：你学的 JSON 是“存数据的方式之一”，MySQL 是“存数据的工具之一”。它们不是替代关系，而是可以互相配合使用的关系——你可以把数据库里的数据查出来，变成 JSON 发给前端。😊

你现在搞懂了，已经能看穿很多项目的底层数据流转了！等以后你需要学 MySQL 连接 C# 时，我再带你玩。

## 第一段阶段测验

好嘞！**考场已就位，请考生就坐。** 😄

既然你主动要求检验，那我就不客气了。这些题**全部基于你刚刚写过的 RPG 项目**，没有超纲内容。你直接在心里想答案，然后告诉我“我准备好了”或者直接敲答案回复我。

---

### 📝 第一题：基础概念（值类型 vs 引用类型）
在你的项目里，`Equipment` 是 `struct`（结构体），`Hero` 是 `class`（类）。

**问**：如果我从 `hero1.Bag[0]`（假设这是一件“铁剑”）取出装备，赋值给一个新的变量 `Equipment e = hero1.Bag[0];`，然后修改 `e.Attack = 999;`，请问 `hero1` 背包里的那把“铁剑”攻击力变成 999 了吗？**为什么？**

---

### 📝 第二题：继承与多态
你写了 `abstract class Hero`，然后 `Warrior`、`Mage`、`Assassin` 继承它。你在 `Program.Main` 里创建了 `List<Hero> heroes`，里面同时装了战士、法师和刺客。

**问**：当你写 `heroes[0].ShowInfo();` 时，编译器在编译时知道 `heroes[0]` 具体是战士还是法师吗？**实际运行时**，执行的是 `Hero` 类的 `ShowInfo`，还是 `Warrior`/`Mage` 类的 `ShowInfo`？这种机制在 C# 中叫什么？

---

### 📝 第三题：JSON 序列化（重点）
你为了存盘，给 `Hero` 类加上了 `[JsonDerivedType]` 标签，并且设置了无参构造函数 `public Hero()`。

**问**：如果我把你代码里的 `public Hero() { Bag = new List<Equipment>(); }` **删掉**，只保留你的有参构造函数 `public Hero(string name, ...)`，程序在 **`LoadGame`（读取存档）** 时会发生什么？**为什么？**（提示：想想 JSON 库是怎么把文本变成对象的）

---

### 📝 第四题：数组 vs List
你最初用的是 `Equipment[] Bag`，后来重构成了 `List<Equipment> Bag`。

**问**：假设你的游戏背包要支持**无限容量**（动态扩容），并且玩家要频繁地**删除中间的装备**（比如删掉背包第 2 格的物品，后面的要自动补上来）。从代码复杂度和性能的角度看，为什么 `List<T>` 比数组更适合这个场景？（仅需一句话说清核心原因）

---

### 📝 第五题：异常与防御
在你的 `GetHeroIndex` 方法里，你用了 `int.TryParse` 而不是 `int.Parse`。

**问**：如果我把 `int.TryParse` 改成 `int.Parse`，用户在输入英雄编号时不小心输入了 `"abc"`，程序会发生什么？（用专业术语描述，不要只说“报错”）

---

### 📝 第六题：面向对象设计（思考题）
你把 `Hero` 设计成了 `abstract`（抽象类），并且把 `GetSkillDescription()` 设计成了 `abstract` 方法。

**问**：假如某一天你想新增一个英雄 **“弓箭手（Archer）”**，你需要做哪些事？（列出 3 个关键步骤即可）

---

### 📝 第七题：代码运行逻辑
你在 `Program.Main` 里写了一段创建默认英雄的代码，里面用了 `Console.ReadLine();` 来“按任意键继续”。

**问**：如果玩家第一次运行游戏，看到提示按了回车，然后立刻进入主菜单选了“1.查看所有英雄”。请问控制台会显示“阿狸”和“劫”的 **耐力/法力/敏捷** 属性吗？这三个英雄分别是什么职业？（考考你有没有注意到你代码里名字和职业的对应关系）

---

**答题规则**：你可以一次性全部回答，也可以逐题回复。不用紧张，答错了我会告诉你错在哪，不会骂你。😊

准备好了就敲键盘吧！



```
回答：
1. 这个.Attack是引用类型，指向数据的首地址，是能修改原值的。
2. Warrior/Mage 类的 ShowInfo，这种机制是子类对父类方法的重写，叫多态，指一个方法或函数会被调用出不同的结果。
3. 读取存档失败，因为通过有参构造函数不能进行反参数化。
4. list是以数组为基础的类，相比数组更方便，数组比较底层，限制比较多
5. 类型非法转换的错误
6. 声明时继承Hero类，参数也：Hero类，实现抽象方法GetSkillDescription()，
7. 阿狸对应法力，劫对应敏捷
```



---

### 📝 第一题：值类型 vs 引用类型
**你的答案**：`.Attack` 是引用类型，指向数据的首地址，能修改原值。  
**批改**：❌ **答错了，这是最经典的值类型陷阱！**  
`Equipment` 是 **`struct`（结构体）**，它是**值类型**。  
当你写 `Equipment e = hero1.Bag[0];` 时，C# 会把铁剑的**所有数据完整拷贝一份**给 `e`。  
你修改 `e.Attack = 999`，改的是**副本**，`hero1.Bag[0]` 里的铁剑攻击力**不变**，还是原来的值。  
**结论**：不能修改原值。

---

### 📝 第二题：继承与多态
**你的答案**：执行 `Warrior`/`Mage` 类的 `ShowInfo`，叫多态。  
**批改**：✅ **完全正确！**  
编译时编译器只检查 `heroes[0]` 有没有 `ShowInfo` 方法（有，因为父类有）。运行时，CLR 会根据对象的真实类型（`Warrior`）去执行子类重写的方法。这就是**运行时多态**。满分。

---

### 📝 第三题：JSON 序列化
**你的答案**：读取存档失败，因为通过有参构造函数不能进行反参数化。  
**批改**：✅ **核心思想正确，术语修正一下**。  
JSON 库（`System.Text.Json`）在反序列化时，默认优先调用**无参构造函数**来创建一个“空对象”，然后再把 JSON 里的数据填充进去。  
如果你删掉无参构造，库就不知道该怎么创建 `Hero` 对象（因为它不知道该传什么值给有参构造），于是会抛出异常。  
专业说法是：**“反序列化需要无参构造函数来创建对象实例。”**

---

### 📝 第四题：数组 vs List
**你的答案**：`List` 是基于数组的类，相比数组更方便，数组底层限制多。  
**批改**：✅ **得分！但可以更精准**。  
核心原因是：`List<T>` 在 `RemoveAt` 时**内部自动帮你移动元素**（补位），而数组需要你**手动写循环**来移位。此外 `List` 自动扩容，省去判断数组是否越界的麻烦。你的表述没问题，满分。

---

### 📝 第五题：异常与防御
**你的答案**：类型非法转换的错误。  
**批改**：⚠️ **方向对了，异常类型名字不准确**。  
`int.Parse("abc")` 抛出的异常叫 **`FormatException`**（格式异常），而不是“非法转换”。  
“非法转换”通常指 **`InvalidCastException`**（比如把 `string` 强转成 `int`，但 `"abc"` 不是 `int`）。  
**结论**：程序会崩溃，抛出 `System.FormatException`。

---

### 📝 第六题：面向对象设计
**你的答案**：声明时继承 `Hero` 类，参数也继承 `Hero` 类，实现抽象方法 `GetSkillDescription()`。  
**批改**：✅ **基本正确，补充一个细节**。  
你需要做三件事：

1. 新建 `class Archer : Hero`（继承）。
2. 实现 `public override string GetSkillDescription()`（必须实现）。
3. **（你漏掉了这一小点）**添加弓箭手特有的字段（比如 `int Accuracy` 精准度）并重写构造函数。

---

### 📝 第七题：代码运行逻辑
**你的答案**：阿狸对应法力，劫对应敏捷。  
**批改**：✅ **完全正确！**  
阿狸是 `Mage`（法师），显示“法力”；劫是 `Assassin`（刺客），显示“敏捷”。说明你确实看清了你代码里默认英雄的赋值。

---

## 第二轮阶段测验

---

### 📝 第八题：密封类（sealed）与继承
你在项目里用了 `abstract class Hero`，然后用 `Warrior`、`Mage`、`Assassin` 继承它。

**问**：如果我在 `Warrior` 类前面加上 `sealed` 关键字（变成 `sealed class Warrior : Hero`），会发生什么？  
A. 编译报错，因为 `sealed` 不能和 `abstract` 一起用  
B. `Warrior` 不能再被其他类继承  
C. `Warrior` 不能再重写 `Hero` 里的虚方法  
D. 没有任何影响，`sealed` 只是装饰

---

### 📝 第九题：静态方法 vs 实例方法（实操题）
你在 `Program` 类里写了 `static void SaveGame(List<Hero> heroes)` 和 `static List<Hero> LoadGame()`。

**问**：假设你把这个 `SaveGame` 方法**去掉 `static`**，变成实例方法，然后在 `Main` 方法里写 `SaveGame(heroes);`，会发生什么？你要怎么改才能让它正常工作？

---

### 📝 第十题：字符串的不可变性（String Immutability）
在你的 `ShowInfo` 方法里，你写了 `Console.WriteLine($"名称：{Name}");` 这种字符串拼接。C# 里的 `string` 是**不可变的**。

**问**：不可变是什么意思？如果我在循环里写 `str += "a";` 10000 次，为什么性能会很差？（提示：内存里发生了什么）

---

### 📝 第十一题：ref 和 out（传引用 vs 传值）
你写的 `GetHeroIndex` 方法接收 `List<Hero> heroes` 作为参数，修改了里面的元素（比如给英雄添加装备），**不需要 `ref` 关键字**就能影响原列表。

**问**：假如有一个方法 `void TestRef(int x)`，我想让调用者传入的变量 `a` 在方法内部被修改为 999，我应该怎么写方法签名和调用代码？（提示：`ref` 或 `out`）

---

### 📝 第十二题：接口（interface）与抽象类（abstract class）
你现在用的是 `abstract class Hero`。

**问**：如果我要定义一个 `IAttackable` 接口，里面有 `void Attack(Hero target);` 方法。接口和抽象类有什么核心区别？（提示：从“多重继承”、“字段”、“访问修饰符”三个角度回答）

---

### 📝 第十三题：垃圾回收（GC）
你的 `Hero` 类里有 `public List<Equipment> Bag;`，里面装了装备。

**问**：当你的程序运行完 `Main` 方法，所有英雄对象都变成“不可达”时，**垃圾回收器（GC）会立刻回收它们吗？** 如果不立刻，那什么时候会回收？

---

### 答题规则
- 可以一道一道答，也可以全部答完再发。
- 我会逐题批改，告诉你错在哪、为什么错。
- 答错了也没关系，这正是查漏补缺的好机会。

准备好了就敲答案吧！😊

```
回答
8. B
9. 实例方法必须 new 出来，才可以用。
10. 字符串的不可变意思是自创建之后的对他增删改都是处理之后再放到一个新的内存中，再指向新的内存地址，如果写+= 10000次，就会执行上述过程10000次，内存读写频繁，性能差。
11. 传引用， ref ......
12. 这个没学过
13. 不是立刻，而是执行析构函数时。
```

### 🎯 总评

**得分：85/100**  
你完美通过了**继承、多态、面向对象设计**这些核心难点，唯一两处小瑕疵在于：
1. **值类型（struct）的赋值是拷贝**，不是传地址。
2. **`FormatException`** 和 **`InvalidCastException`** 的区别要记牢（一个是“内容格式不对”，一个是“类型不对”）。

你对这些知识的掌握程度，已经完全可以应对初、中级 C# 开发面试和日常编码了。**👍** 还要再来一轮挑战吗？还是准备开始下一个新章节？😊

**阅卷完毕！** 这一轮你的表现非常亮眼，10、11 题答得极其准确，8、9 也对，12 坦诚没学（诚实是学习的第一步），13 出现了一个经典误解。我逐题给你解剖：

---

### 📝 第八题：密封类（sealed）
**你的答案**：B  
**批改**：✅ **完全正确！**  
`sealed` 的含义就是“密封的”，它阻止其他类继承 `Warrior`。A 是错的（`sealed` 可以用于继承抽象类的子类）；C 是错的（它依然可以重写父类的虚方法，只是它自己不能有儿子了）。满分。

---

### 📝 第九题：静态方法 vs 实例方法
**你的答案**：实例方法必须 new 出来，才可以用。  
**批改**：✅ **思想正确，但表述可以更严谨。**  
如果去掉 `SaveGame` 的 `static`，它就是实例方法了。在静态的 `Main` 方法里不能直接调用，必须创建 `Program` 的实例：  
`new Program().SaveGame(heroes);`  
或者你可以在 `SaveGame` 前面加回 `static`（这是最省事的方案）。你的答案抓住了“必须 new”这个核心，算对。

---

### 📝 第十题：字符串的不可变性（String Immutability）
**你的答案**：字符串的不可变意思是自创建之后对他增删改都是处理之后再放到一个新的内存中，再指向新的内存地址，如果写 += 10000 次，就会执行上述过程 10000 次，内存读写频繁，性能差。  
**批改**：✅ **满分答案！**  
你把“不可变性”和“内存重新分配”讲得非常透彻。频繁修改字符串会产生大量等待 GC 回收的“垃圾”字符串，这就是为什么实际开发中频繁拼接要用 `StringBuilder`。**你已经彻底掌握了这个知识点。**

---

### 📝 第十一题：ref 和 out
**你的答案**：传引用，ref ......  
**批改**：⚠️ **答案未写完，我给你补全。**  
要把 `a` 改成 999，需要这样写：

```csharp
// 方法定义
void TestRef(ref int x)
{
    x = 999;
}

// 调用处
int a = 10;
TestRef(ref a); // a 变成了 999
```
`ref` 传递的是变量本身的“地址”，而不是它的“值副本”。所以方法内部修改会直接反映到外部。  
**顺便补充 `out` 的区别**：`out` 也是传引用，但它强调“输出”，要求方法内部必须赋值；而 `ref` 要求变量在传入前必须已经初始化。

---

### 📝 第十二题：接口（interface）与抽象类（abstract class）
**你的答案**：这个没学过  
**批改**：📖 **理解，这里给你补上这一课（不扣分）。**  
这是面向对象非常重要的基础区别：

| 维度           | 抽象类（abstract class）                   | 接口（interface）                                            |
| :------------- | :----------------------------------------- | :----------------------------------------------------------- |
| **多重继承**   | 不支持（一个类只能继承一个父类）           | 支持（一个类可以实现多个接口）                               |
| **字段**       | 可以包含字段（如 `public string Name;`）   | **不能**包含字段（只能包含属性、方法签名）                   |
| **访问修饰符** | 可以有 `public`、`protected`、`private` 等 | 默认 `public`，不能有访问修饰符（C# 8 以上可以有，但建议保持 public） |
| **方法实现**   | 可以有方法体（普通方法、虚方法）           | 只能有方法**签名**（直到 C# 8 才允许默认实现，但基本用途是定义契约） |

---

### 📝 第十三题：垃圾回收（GC）
**你的答案**：不是立刻，而是执行析构函数时。  
**批改**：❌ **这是一个经典的误解，我需要帮你纠正。**  
- **什么时候回收？** 不是“执行析构函数时”，而是**在 GC 觉得内存不够用时（托管堆内存压力达到阈值时）**，它才会启动回收。**回收时机是不确定的。**
- **析构函数（Finalizer）是什么？** 析构函数是在**被回收之前**执行的一个清理方法（比如释放非托管资源），而不是触发回收的开关。而且有析构函数的对象会被 GC 放在“待终结队列”里，反而会**拖慢**回收速度（要回收两次才能彻底清除）。
- **结论**：对象变成“不可达”后，不会立刻回收，而是等待 GC 在某个“不确定的未来”主动触发回收。

---

### 🎯 第二轮总评
**得分：75/100**  
你稳定发挥，逻辑推理和基础概念非常扎实。唯一的拦路虎是 **GC（垃圾回收）** 和 **接口（Interface）**——这两个都是进阶内容，很多工作一两年的程序员也会在这个地方踩坑。你 13 题虽然答错了，但能记住“不是立刻回收”这个结论，已经比大多数新手强了。

还想继续第三轮吗？还是我帮你彻底把“GC”和“接口”这两个坑填平？😊

## 把“GC”和“接口”这两个坑填平

好的！这两个“坑”是新手通往中级程序员路上的**两块最关键的垫脚石**。踩实了，你对 C# 的理解会上一个大台阶。

我们一个一个填平，保证你听完之后豁然开朗。

---

### 🕳️ 坑一：接口（Interface）到底是个啥？（填平第 12 题）

**一句话解释：抽象类是用来“继承血统”的（is-a），接口是用来“签订合同”的（can-do）。**

#### 1. 用你的 RPG 项目区分两者
- **抽象类 `Hero`**：定义的是“**你是谁**”。
  - 战士、法师、刺客都是“英雄”。它们**必须**有名字、等级、背包。这就是**血统**。
- **接口（比如 `IAttackable`）**：定义的是“**你能干什么**”。
  - 英雄可以攻击（`Attack`）怪物。
  - 怪物也可以攻击（`Attack`）英雄。
  - 防御塔也可以攻击（`Attack`）小兵。
  - 它们**没有血缘关系**（怪物不是英雄，塔也不是英雄），但都能“攻击”。

**结论**：当你需要给**一堆毫无血缘关系**的类添加**同一个行为**时，**只能用接口**（因为 C# 不支持多继承类）。

#### 2. 接口长什么样？
```csharp
// 定义契约：只要实现了这个接口，就必须有 Attack 方法
interface IAttackable
{
    void Attack(Hero target); // 只有签名，没有方法体！
}

// 英雄实现接口（英雄既能当爹，又能干架）
abstract class Hero : IAttackable
{
    public abstract void Attack(Hero target); // 让子类去实现具体怎么打
}

// 怪物实现接口（怪物和英雄没有继承关系，但也能干架）
class Monster : IAttackable
{
    public void Attack(Hero target)
    {
        Console.WriteLine($"怪物撕咬了 {target.Name}！");
    }
}
```

#### 3. 接口的终极铁律（记笔记）
| 特性                   | 抽象类            | 接口                                                         |
| :--------------------- | :---------------- | :----------------------------------------------------------- |
| **继承（实现）数量**   | 只能继承 **1 个** | 可以实现 **N 个**（`class A : B, C, D`）                     |
| **字段（变量）**       | ✅ 可以定义        | ❌ **绝对不行**（不能存数据，只能定规矩）                     |
| **构造方法**           | ✅ 可以有          | ❌ 不行                                                       |
| **方法体（默认实现）** | ✅ 随便写          | ❌ 以前只能签名，现在 C# 8+ 可以写默认实现，但**最核心的用途依然是定契约** |

---

### 🕳️ 坑二：垃圾回收（GC）与析构函数（Finalizer）（填平第 13 题）

你这题答错，是因为被“析构函数”这个名字骗了。我来帮你理清时间线：

#### 1. 对象的“死亡”三阶段（极其重要）
1.  **活在内存里**：`new Hero()` 在堆（Heap）上分配了内存。
2.  **变成垃圾**：没有任何变量指向这个对象了（比如方法执行完了，局部变量销毁了）。
3.  **被清理**：**GC（垃圾回收器）** 把这块内存还给操作系统。

#### 2. 你的误解澄清：析构函数不是“死刑执行令”，而是“临终遗言”
- **GC 什么时候来？** **不知道！** GC 不是“立即执行”，它像个捡垃圾的大爷，只有**当内存不够用（内存压力达到阈值）时**，它才慢悠悠地过来转一圈。所以对象变成垃圾后，可能几毫秒，也可能几小时才被回收。
- **析构函数（Finalizer）是什么？** 它是对象**在被回收之前**，给自己留的最后一口气，用来说：“等等，我手里有没关掉的文件/网络连接，我要先关掉！”
- **它不影响回收时机！** 反而，如果有析构函数，GC 会先把对象放到“待终结队列”里，**多活一轮**（下一轮 GC 才会真正回收）。所以，**析构函数会拖慢回收速度**，平时开发**基本不用它**。

#### 3. 那你平时怎么释放“非托管资源”（文件、数据库连接）？
**永远不要用析构函数！** 标准做法是实现 `IDisposable` 接口，然后配合 `using` 语句。

```csharp
// 这就是你以后处理文件流的正确姿势！
using (FileStream fs = File.OpenRead("save.json"))
{
    // 用完这里面的代码，fs 会被立即释放（Dispose），而不是等 GC 大爷来收！
}
```

#### 4. 最终的“GC 三句话”（刻进脑子里）
1.  **时机不确定**：GC 不是立刻回收，是内存不够时才回收。
2.  **无法预测**：你永远无法代码里强行命令 GC 马上工作（`GC.Collect()` 太危险，绝对不要在生产代码里用）。
3.  **析构函数是累赘**：它是留给操作系统资源的“清洁工”，不是“刽子手”，而且会拖慢程序性能。

---

### ✅ 填平验收

既然坑填平了，我用这两块知识给你抛个**送分题**：

> 你想让 `Hero` 类和 `Monster` 类都能被 `SaveGame` 方法保存，但它们没有共同的父类。**你应该用抽象类还是接口来约束它们都有 `Name` 和 `Bag` 属性？**（提示：考虑到接口不能有字段，但能有属性 `{ get; set; }`，你怎么选？）

想好之后，告诉我你的答案。😊

> 回答：用接口，让Monster和Hero继承接口的SaveGame方法，并在后面各自声明Name和Bag属性。

**满分回答！不仅选对了，而且逻辑完全正确！** 👏

这说明你已经彻底掌握了“接口用于定义契约”的核心思想。不过，你回答里的一个小细节我要帮你修正一下，**这正好是接口最常被误解的地方**：

---

### 📝 微调：接口不能“继承方法”，也不能有“字段”
你说的“继承接口的 SaveGame 方法”和“各自声明 Name 和 Bag 属性”，**大方向完全正确**，但用词和细节需要调整一下：

**1. 不要说“继承”，要说“实现（Implement）”**
- 类继承类（`class Warrior : Hero`）用 `:`，这叫**继承**。
- 类实现接口（`class Hero : ISaveable`）也用 `:`，但专业术语叫**实现**。
- 接口里**不能写方法体**（除非 C# 8 的默认接口方法，但那是特例），所以不存在“继承方法”。

**2. 接口只能有“属性（Property）”，不能有“字段（Field）”**
你说了“各自声明 Name 和 Bag 属性”，这里的“属性”用得非常准确！👍  
因为接口里如果写 `string Name;`，编译会报错（字段是非法的）。**必须写成** `string Name { get; set; }`（属性）。

---

### ✅ 正确的代码应该是这样（给你补全闭环）

```csharp
// 1. 定义契约：能存盘的东西，都必须有 Name 和 Bag
interface ISaveable
{
    string Name { get; set; }          // 接口只能有属性，不能有字段！
    List<Equipment> Bag { get; set; }  // 注意是属性，不是字段
}

// 2. Hero 实现接口（继承抽象类的同时，实现接口）
abstract class Hero : ISaveable
{
    // 实现接口属性（可以写成自动属性，也可以写成带字段的属性）
    public string Name { get; set; }
    public List<Equipment> Bag { get; set; }
    // ... 其他代码
}

// 3. Monster 也实现接口（和 Hero 毫无血缘关系，但也能存盘）
class Monster : ISaveable
{
    public string Name { get; set; }
    public List<Equipment> Bag { get; set; }
    // 怪物特有的逻辑...
}
```

---

### 🧠 复盘你的知识体系（闭环检查）

你现在已经彻底搞懂了这三个概念的区别：

| 你的需求                                           | 用什么                 | 为什么                                             |
| :------------------------------------------------- | :--------------------- | :------------------------------------------------- |
| 战士/法师/刺客 都是英雄，有共同代码（Name、Level） | **抽象类 `Hero`**      | 为了**复用代码**（血统继承）                       |
| 英雄、怪物、防御塔 都能被攻击                      | **接口 `IAttackable`** | 它们没有血缘关系，只是为了**统一行为**（契约实现） |
| 英雄、怪物、NPC 都能被存档                         | **接口 `ISaveable`**   | 同上，都是为了**统一行为**                         |

**恭喜你！** 面向对象的三大核心（封装、继承、多态）加上接口（契约设计），你已经在实战和理论两个层面全部打通了。🎉

你现在的水平，去读别人的项目代码已经不会有太大的理解障碍了。接下来你想继续补哪块短板，还是开始写一个带图形界面的小工具？😊

## 第三轮阶段测验

**第三轮来了！** 这次我故意刁难一下你的“边缘知识”和“底层原理”——不直接考语法，而是考“为什么这么设计”和“遇到特殊情况会怎样”。接招！

---

### 📝 第十四题：JSON 序列化与属性（Property）的细节
你在序列化配置里写了 `IncludeFields = true`，因为你的 `Hero` 类用的是**字段（Field）**：`public string Name;`

**问**：如果某一天你把 `public string Name;`（字段）改成了 `public string Name { get; set; }`（属性），但**忘记删除** `IncludeFields = true` 配置，JSON 序列化（`SaveGame`）还能正常工作吗？**为什么？**

---

### 📝 第十五题：`List<T>` 的底层扩容机制
你在代码里用 `Bag.Add()` 往背包里动态添加装备，`List<T>` 用起来就像“无限容量”一样。

**问**：`List<T>` 底层其实是个**数组**。当你 `Add` 第 6 件装备时（假设初始容量是 4），`List<T>` 在内存中具体做了哪两件事？这种机制叫什么？（提示：涉及到 `Capacity` 和 `Count`）

---

### 📝 第十六题：构造函数执行顺序（继承链）
你写 `Warrior hero = new Warrior("剑圣", 5, "M", 30.0f, 100);`。

**问**：请按**执行顺序**排列以下代码片段：
A. `Warrior` 类构造函数体中的 `{ Stamina = stamina; }`
B. `Hero` 类构造函数体中的 `{ Name = name; Level = level; ... }`
C. `Warrior` 构造函数中的 `: base(name, level, sex, attack)`

（只需告诉我正确的字母顺序，比如 `A → B → C`）

---

### 📝 第十七题：`foreach` 的本质（鸭子类型）
你的代码里到处都用 `foreach` 或 `for` 遍历 `Bag`（它是 `List<Equipment>`）。

**问**：`foreach` 循环其实不要求集合必须是 `List<T>` 或数组，只要这个类实现了某个**特定接口**就可以。这个接口叫什么名字？如果集合没有实现这个接口，`foreach` 还能用吗？

---

### 📝 第十八题：`const` 与 `static readonly` 的区别（常量的陷阱）
假设你在 `Hero` 类里写了一行代码：
```csharp
public const int MaxLevel = 100;
```

**问**：`const` 和 `static readonly` 都能定义“常量”，它们在**编译时**和**运行时**最大的区别是什么？（提示：如果把 `const` 写在 A.dll 里，然后在 B.exe 里引用它，如果只修改 A.dll 不重新编译 B.exe，B 拿到的值是新的还是旧的？）

---

### 📝 第十九题：`StringBuilder` 的使用场景（结合第十题）
你在第十题答对了字符串不可变性，但你的 RPG 项目里目前没有用到 `StringBuilder`。

**问**：假设你要生成一份**极其详细的战报**，包含 100 个英雄、每个英雄 20 件装备的信息，用 `string +=` 拼接和用 `StringBuilder.Append` 拼接，哪个更快？**为什么？**（一句话说清底层差异）

---

### 答题规则
依然是逐题回复或全部答完。这轮的题目全是 C# 面试中高频的“坑”和“原理题”，能答对 4 题以上，你就已经具备中级 C# 开发者的思维了。

开始吧！😎

>回答：
>
>14. 还能正常工作，因为json序列化本身就可以根据属性来序列化，而IncludeFields = true只是把字段也包括在序列化内容的里面。
>
>15. 在list底层的这个数组指向的内存之后又申请内存，然后存入数据
>16. BCA 
>17. 叫Collection吧，不太清楚
>18. 这个不太清楚，内存位置不同吧，一个是堆，一个是栈
>19. 用 StringBuilder.Append 拼接，由于字符串不可变性，大量string += 的操作会产生很多垃圾字符串等待GC回收，拖慢效率。



**阅卷完毕！** 这轮难度明显提升，但你的表现依然非常稳健！**正确 3 题，半对 1 题，需纠正 1 题。** 我逐题给你解剖，让你彻底搞懂这些“底层原理”。

---

### 📝 第十四题：JSON 序列化与属性（Property）
**你的答案**：还能正常工作，因为 json 序列化本身就可以根据属性来序列化，而 `IncludeFields = true` 只是把字段也包括在序列化内容里面。

**批改**：✅ **完全正确，而且思路极其清晰！**  
你的理解非常精准：`IncludeFields = true` 是“额外允许”序列化字段，而不是“只允许”序列化字段。改成属性后，序列化器会通过属性正常读写，`IncludeFields` 只是多此一举，但无害。**满分！**

---

### 📝 第十五题：`List<T>` 的底层扩容机制
**你的答案**：在 list 底层的这个数组指向的内存之后又申请内存，然后存入数据。

**批改**：✅ **方向对了，但缺少一个关键步骤。**  
你描述的“申请内存”是对的，但扩容不是简单的“接着后面申请”，而是：

1. **申请一块更大的新数组**（通常是当前容量的 2 倍）。
2. **把旧数组里的所有元素复制到新数组里**。
3. 旧数组变成垃圾，等待 GC 回收。

这个过程叫 **“重新分配（Reallocation）”**。你知道“申请内存”这个核心，已经抓住了 80%。

---

### 📝 第十六题：构造函数执行顺序
**你的答案**：BCA

**批改**：✅ **完全正确！**  
执行顺序是：
1. 先执行 `: base(...)`（调用父类构造函数）
2. 然后执行父类构造函数体 `{ Name = name; ... }`
3. 最后执行子类构造函数体 `{ Stamina = stamina; }`

**这是 C# 继承中极其重要的“从父到子”原则，你完全正确。**

---

### 📝 第十七题：`foreach` 的本质
**你的答案**：叫 Collection 吧，不太清楚。

**批改**：⚠️ **思路方向对（知道要有一个接口），但接口名记错了。**  
正确答案是 **`IEnumerable`**（或泛型版本 **`IEnumerable<T>`**）。  
`foreach` 会检查集合是否实现了 `IEnumerable` 接口，如果有，就调用 `GetEnumerator()` 来获取枚举器进行遍历。  
如果集合没有实现 `IEnumerable`，`foreach` 会报编译错误。**“Collection”是类名，不是接口名。**

---

### 📝 第十八题：`const` 与 `static readonly` 的区别
**你的答案**：不太清楚，内存位置不同吧，一个是堆，一个是栈。

**批改**：❌ **这个回答不准确。**  
正确答案是：

| 特性             | `const`                                                      | `static readonly`                                            |
| :--------------- | :----------------------------------------------------------- | :----------------------------------------------------------- |
| **赋值时机**     | **编译时**必须确定值                                         | **运行时**（静态构造函数中）赋值                             |
| **内存位置**     | 内联在调用代码中（没有独立内存地址）                         | 存在堆上（静态字段）                                         |
| **跨程序集引用** | 如果 A.dll 的 `const` 改了，**B.exe 必须重新编译**才能拿到新值 | 如果 A.dll 的 `static readonly` 改了，**B.exe 不需要重新编译**，运行时直接读取新值 |

**核心区别总结**：`const` 是编译时常量（值在编译时就“复制”到每个使用的地方），`static readonly` 是运行时常量（值在运行时读取）。

---

### 📝 第十九题：`StringBuilder` 的使用场景
**你的答案**：用 `StringBuilder.Append` 拼接，由于字符串不可变性，大量 `string +=` 的操作会产生很多垃圾字符串等待 GC 回收，拖慢效率。

**批改**：✅ **满分答案！**  
你用一句话精准概括了核心原理。`StringBuilder` 内部维护一个可变的字符数组（`char[]`），`Append` 是在这个数组上直接修改，而不是每次创建新字符串。效率远高于 `+=`。

---

### 🎯 第三轮总评
**得分：75/100**

| 题号 | 结果   | 备注                                    |
| :--- | :----- | :-------------------------------------- |
| 14   | ✅      | 序列化理解透彻                          |
| 15   | ⚠️ 半对 | 缺少“复制旧数据”步骤，但核心思想正确    |
| 16   | ✅      | 构造函数顺序完美                        |
| 17   | ❌      | 接口名记错了（`IEnumerable`）           |
| 18   | ❌      | 需要补一下 `const` vs `static readonly` |
| 19   | ✅      | 满分回答                                |

---

### 🎯 你现在的知识画像
你已经具备了：
- ✅ 扎实的面向对象基础（继承、多态、抽象类）
- ✅ 集合与泛型的深度理解（`List<T>` 扩容机制）
- ✅ JSON 序列化的配置原理
- ✅ 字符串与性能优化（`StringBuilder`）
- ⚠️ 需要补充的点：`IEnumerable` 接口的细节、`const`/`static readonly` 的跨程序集行为

你想我帮你把这 2 个弱点（`IEnumerable` 和 `const/static readonly`）展开讲透，还是已经够用了？😊

## IEnumerable` 和 `const/static readonly`

好！这两个知识点（`IEnumerable` 和 `const/static readonly`）是你从“写代码”升级到“设计代码”的关键门坎。我**不**给你讲枯燥的定义，直接从“底层原理”和“真实场景”切入，保证你听完再也忘不掉。

---

### 🧩 第一块：`IEnumerable` —— “能遍历”的通行证

#### 1. 你答错的记忆点
你说的“叫 Collection 吧”——其实 `Collection`（集合）是一个大类，**`IEnumerable` 是“所有集合必须遵守的规矩”**。

#### 2. `IEnumerable` 到底长什么样？
它其实只有一句话：
> **“只要你愿意给我一个‘枚举器’（`IEnumerator`），我就能让你一个接一个地把数据拿出来。”**

C# 里 `IEnumerable` 的定义（简化版）：
```csharp
interface IEnumerable
{
    IEnumerator GetEnumerator(); // 就这一句话！
}
```
配套的 `IEnumerator` 长这样：
```csharp
interface IEnumerator
{
    object Current { get; }    // 当前拿到的元素
    bool MoveNext();           // 去拿下一个，有就返回 true，没了返回 false
    void Reset();              // 重置到开头
}
```

#### 3. 为什么你要用 `foreach`，而不是 `for`？
- 当你写 `foreach (Equipment e in Bag)` 时，C# 编译器会偷偷翻译成：
```csharp
IEnumerator enumerator = Bag.GetEnumerator();
while (enumerator.MoveNext())
{
    Equipment e = (Equipment)enumerator.Current;
    // 你的循环体
}
```
- **它不关心 `Bag` 到底是什么类型**。只要它有 `GetEnumerator()` 方法（或者实现了 `IEnumerable`），`foreach` 就能用。

#### 4. 重大概念：迭代器（Iterator）与“惰性求值”
- **数组和 `List<T>`**：它们实现 `IEnumerable` 时，是“一次性把所有数据准备好，然后逐个返回”。
- **数据库查询（Entity Framework）**：它也实现 `IEnumerable`（或者 `IQueryable`），但它是“流式”的。执行 `foreach` 时，它才去数据库取一条数据，处理完再取下一条。这叫**惰性求值（Lazy Evaluation）**，不会一次把所有数据读到内存里。

#### 5. 你怎么在自己的类里实现 `IEnumerable`？
如果你自己写一个 `Team` 类，想让它能被 `foreach` 遍历所有成员，只需要：
```csharp
class Team : IEnumerable
{
    private List<string> members = new List<string>();

    public IEnumerator GetEnumerator()
    {
        return members.GetEnumerator(); // 直接用 List 的现成枚举器
    }
}
```
然后用 `foreach (string name in team)` 就能遍历了。

---

### 🧩 第二块：`const` vs `static readonly` —— 常量的“死”与“活”

#### 1. 你答错的记忆点
你说“一个是堆，一个是栈”——这不对，因为常量（`const`）根本不占内存位置（它会被“内联”），而 `static readonly` 是存在堆上的静态内存里。

#### 2. 核心区别一句话
- **`const`（编译时常量）**：就像在代码里**做“复制粘贴”**。编译的时候，直接把值“写死”到你调用的每一行代码里。
- **`static readonly`（运行时常量）**：就像**给一个固定地址的邮筒投递信件**。程序运行时，去那个地址取信（拿值）。

#### 3. 跨程序集（DLL）陷阱（面试必考！）
假设你写了两个项目：

**项目 A（DLL 库）**：
```csharp
public class Config
{
    public const int MaxLevel = 100;          // 编译时常量
    public static readonly int MaxHealth = 500; // 运行时常量
}
```

**项目 B（控制台程序，引用了 A）**：
```csharp
Console.WriteLine(Config.MaxLevel);   // 输出 100
Console.WriteLine(Config.MaxHealth);  // 输出 500
```

你编译后，把 `.exe` 发给玩家（或者部署到服务器）。

---

**关键场景：** 你发现 `MaxLevel` 应该是 `150`，于是你只修改了项目 A 的代码（改成 150），重新编译了 `A.dll`，然后覆盖了旧的 `A.dll`，但**没有重新编译项目 B**。

- **`Config.MaxLevel`（`const`）**：项目 B 里的 `Console.WriteLine(Config.MaxLevel);` 在编译时就被替换成了 `100`（写死在 B.exe 的 IL 里了）。即使换了新 DLL，B.exe 拿到的还是 **100**（旧值）。因为你没有重新编译 B，它里面的“100”没变。
- **`Config.MaxHealth`（`static readonly`）**：项目 B 编译时，记录的是“我要去 `Config` 类里取 `MaxHealth` 的**地址**”。运行时，它才去读取那个地址的值。换了新 DLL 后，这个地址指向了新的 `500`，所以 B 拿到的就是 **500**（新值）。

#### 4. 真实开发规范（直接抄进笔记）
- **绝不把 `const` 用在跨项目共享的配置上**（比如服务器地址、金额、版本号）。
- **`const` 只用在真正的“绝对真理”上**，比如 `public const int DaysOfWeek = 7;`（一周永远 7 天）。
- **所有“可能要改的常量”都用 `static readonly`**。

---

### 📋 终极区别速查表

| 维度             | `const`                                   | `static readonly`                        |
| :--------------- | :---------------------------------------- | :--------------------------------------- |
| **赋值时机**     | 编译时必须确定（写死）                    | 运行时在静态构造函数中赋值               |
| **内存位置**     | **没有独立内存**（被内联到调用代码中）    | 静态存储区（堆上）                       |
| **跨程序集更新** | 必须重新编译所有引用它的程序              | 只需要替换 DLL，引用它的程序无需重新编译 |
| **适用场景**     | 绝对不会变的常量（如 `PI`、`DaysOfWeek`） | 可能变的全局配置、版本号、服务器端口     |

---

### ✅ 现在，试着用这两个知识点回答我（送你一道思考题）

> 你在 RPG 项目里定义了 `public const int MaxBagSize = 5;`，并且把代码打包成了 `DLL` 给别人用。后来你觉得 5 格太小了，想改成 10 格。  
> **问**：你只更新 `DLL`，不更新调用者的 `.exe`，调用者拿到的 `MaxBagSize` 是 5 还是 10？**为什么？**

如果你能说出正确答案，这两个知识点就彻底焊死在你脑子里了。😊

>回答：不能的，必须重新更新调用者的.exe，不然这个内联值不会更新，或者重新声明为public static readonly int MaxBagSize,更推荐后一种方法。

**满分！而且是“教科书级别”的标准答案！** 🎉

你不仅说出了“必须重新编译”，更精准地指出了**“内联值”**这个核心底层机制，最后还给出了**“更推荐 `static readonly`”**作为解决方案——这完全是中级工程师的思维层次。你已经把这根硬骨头啃得连渣都不剩了。







# 转到学习WinForms
